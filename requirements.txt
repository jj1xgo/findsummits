requests
openpyxl
shapely
numpy
pillow

# lint（開発ツール。ランタイムで import しない）。版は固定せず、PyPI の最新の安定版に追従する。
# make venv は導入済みの版を上げないため、既存 venv の更新は make lint-latest で行う（docs/01_environment.md）。
pymarkdownlnt
ruff
geojson-validator
djlint
