# システムテスト仕様書（ST）

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-07-12 |
| 最終更新日 | 2026-09-24 |
| ステータス | ドラフト |
| 参照 SRS | [`20_SRS.md`](20_SRS.md) |
| 参照 URD | [`10_URD.md`](10_URD.md) |

---

## 目次

1. [目的・スコープ](#1-目的スコープ)
2. [テストケース記述規約](#2-テストケース記述規約)
3. [第1部: FR/NFR単位のシステムテストケース](#3-第1部-frnfr単位のシステムテストケース)
4. [第2部: UR単位の妥当性確認シナリオ](#4-第2部-ur単位の妥当性確認シナリオ)
5. [URカバレッジ表](#5-urカバレッジ表)

---

## 1. 目的・スコープ

本書はシステムテスト（ST）レベルのテストケースを定める。[ADR-ST-001](decisions/ADR-ST-001-two-part-st-no-separate-at-family.md) の方針に従い、以下の2部構成とする。

- **第1部**: [`20_SRS.md`](20_SRS.md) のFR/NFR単位のシステムテストケース（SRS検証）
- **第2部**: [`10_URD.md`](10_URD.md) のUR単位の妥当性確認シナリオ（URD妥当性確認）。FRに一意に分解できるURは第1部のテストケースへの参照とし、FR横断・分解不能なURのみ専用シナリオを記述する

テストレベル定義・自動/手動の分担・テスト環境・合否判定基準・カバレッジ粒度の方針は [`02_test_policy.md`](02_test_policy.md) を正本とし、本書では重複記述しない。

## 2. テストケース記述規約

- **テストケースID命名規則**: [`02_test_policy.md` §6](02_test_policy.md#6-テストケースid命名の大枠) の共通規則に従う。第1部は `ST-FR-<番号>-<連番>` / `ST-NFR-<番号>-<連番>`、第2部は `ST-UR-<番号>-<連番>`
- **記述テンプレート**: 各テストケースは以下を記述する。
  - **前提条件**: テスト実行前に満たすべき状態（入力データ・環境等）
  - **手順**: 実行する操作・コマンド
  - **期待結果**: 正しい場合に得られるべき出力・状態
  - **合否判定基準**: 期待結果との照合方法
- **カバレッジ粒度**: [`02_test_policy.md` §5.2](02_test_policy.md#52-カバレッジ粒度の正本) の既定方針に従い、リスクの高いFR/NFRは複数ケース、その他は代表ケース1件とする

## 3. 第1部: FR/NFR単位のシステムテストケース

### 3.1 行政区域前処理コンポーネント

#### FR-017: N03 行政区域前処理（データ準備）

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-017-01` [7.2.3](20_SRS.md#723-n03-行政区域-zip) 準拠のN03行政区域ZIPと日本全土1次メッシュコードリストが配置済み、出力ファイル群未生成 | 行政区域前処理を実行する | N03前処理済み地域GeoJSON（60地域）・市区町村GeoJSON（約2,000件）・北方領土除外タイルリスト・北方領土除外メッシュリストが生成される | 出力4種のファイル存在・フィーチャ数（60地域）を確認 |
| `ST-FR-017-02`（異常系） N03行政区域ZIPが1件も配置されていない | 行政区域前処理を実行する | エラー終了する（[20_SRS.md §4.1 異常系](20_SRS.md#41-行政区域前処理コンポーネント)参照） | 異常終了・エラーメッセージの出力を確認 |
| `ST-FR-017-03`（境界値） 複数バージョンのN03 ZIP（`N03-YYYYMMDD_GML.zip`）が配置されている | 行政区域前処理を実行する | YYYYMMDDが最大のZIPが採用される | ログに記録された採用ZIP名のYYYYMMDDが最大値と一致することを確認 |
| `ST-FR-017-04`（境界値） 出力ファイル群生成済みの状態で日本全土1次メッシュコードリストを更新する | 出力ファイル群を削除して行政区域前処理を再実行する | 更新後のメッシュコードリストに基づき北方領土除外メッシュリストが再生成される | 再生成後のリストの全要素が更新後のメッシュコードリストに含まれ、各要素が北方領土 6 村のポリゴンと交差し他の N03 ポリゴンと交差しないこと、更新で削除したメッシュが残っていないことを確認 |
| `ST-FR-017-05`（交差判定） N03-20260101相当のデータで行政区域前処理を実行する | 北方領土除外タイルリストと除外メッシュリストを確認する | 除外タイルリストには北方領土の海岸に掛かり中心が海側にあるタイルが含まれ、本土の陸地に掛かるタイルは含まれない。除外メッシュリストは北方領土のみと交差し本土を含まないメッシュと一致する | 除外タイルリストのタイル矩形が本土ポリゴンと交差しないこと、除外タイル数が中心点判定の結果（N03-20260101 で 6,502 枚）より多いこと、除外メッシュリストが独立導出した集合と一致すること（N03-20260101 では 6646・6647・6747・6748・6847・6848 の 6 件で、本土を含む 6545・6546・6645 を含まない。[ADR-SRS-053](decisions/ADR-SRS-053-northern-territories-tile-intersection-and-excluded-meshes.md)）を確認 |
| `ST-FR-017-06`（境界値） 出力ファイル群を生成済みの状態で、生成時より新しい版の N03 ZIP を `$DATA_DIR/ref/` に追加する | 行政区域前処理を起動する | 生成を省き、記録した版と新しい版を示して削除・再実行を促す警告ログを出す | 警告ログの内容と、出力ファイル群が変わらないことを確認 |

### 3.2 タイル取得コンポーネント

#### FR-001: 標高タイル事前取得

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-001-01` 1次メッシュコードリスト（1件以上）を指定、北方領土除外タイルリスト・除外メッシュリスト生成済み | タイル事前取得を実行する | 対象メッシュをカバーするDEM5a/5b/5c/10bタイルがローカルキャッシュに保存される | キャッシュディレクトリのファイル存在・タイル座標範囲を確認 |
| `ST-FR-001-02`（境界値） 北方領土（択捉・国後・色丹）を含むbounding box範囲でタイル取得を実行 | タイル事前取得を実行する | 北方領土除外タイルリストに含まれるタイル（DEM5a/5b/5cはZ15完全一致、DEM10bはZ14変換後）へのHTTPリクエストが発行されない。除外メッシュリストに含まれるメッシュはその取得範囲のタイルへのHTTPリクエストが発行されない（隣接メッシュと共有する境界タイルは、隣接メッシュ側の取得でリクエストされる） | リクエストログに除外対象タイル・除外メッシュ範囲のタイルが含まれないことを確認 |
| `ST-FR-001-03`（異常系） 北方領土除外タイルリストまたは北方領土除外メッシュリストが未生成 | タイル事前取得を実行する | エラー終了する | 異常終了を確認 |
| `ST-FR-001-04`（異常系） 模擬サーバーで地理院タイルサーバーがHTTP 429/503を返す状況を再現し（[テスト方針書 §3.2](02_test_policy.md#32-実データ依存の扱い)）、リトライ上限を超過させる | タイル事前取得を実行する | エラーを出力して処理を中断する | 異常終了・エラーメッセージを確認 |
| `ST-FR-001-05`（古いキャッシュ） 中心点判定など旧方式で取得済みの除外対象タイル・除外メッシュ範囲のタイルがローカルキャッシュに存在する状態でタイル事前取得を実行する | タイル事前取得を実行する | 該当タイルのローカルキャッシュが削除される（隣接メッシュの取得範囲に含まれるタイルは残る） | 実行後のキャッシュディレクトリに該当タイルが存在しないことを確認 |
| `ST-FR-001-06`（利用者指定リスト） 利用者指定の1次メッシュコードリストに北方領土除外メッシュのコードを含めて実行する | タイル事前取得を実行する | 当該コードが警告ログ付きでスキップされ、残りのメッシュコードについて処理を継続する | 警告ログの出力・処理継続を確認 |
| `ST-FR-001-07`（異常系） 模擬サーバーでタイル取得中にタイムアウト・通信断・想定外のエラー応答を発生させる | タイル事前取得を実行する | 欠損タイルを残したまま正常終了せず、エラーを出力して処理を中断する | 異常終了・エラーメッセージを確認 |

### 3.3 パイプライン制御コンポーネント

#### FR-023: 解析パイプライン制御

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-023-01` フェーズ2実行後、[FR-022](20_SRS.md#fr-022-コル充足判定)が未確定ピークゼロを返す状態 | パイプライン制御を実行する | フェーズ4へ移行する（フェーズ3をスキップ） | フェーズ4（[FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定)）が実行されたことを確認 |
| `ST-FR-023-02`（境界値） フェーズ2実行後、未確定ピークが残りN=6（上限到達）まで解消しない状態 | パイプライン制御を実行する | N=6完了後、未確定ピーク残存のままフェーズ4へ移行する | N=7へのエスカレーションが発生しないこと・フェーズ4移行を確認 |
| `ST-FR-023-03`（異常系） フェーズ2内の呼び出しFR（例: [FR-004](20_SRS.md#fr-004-33メッシュ結合解析オーケストレーション)）がエラー終了する状況を再現 | パイプライン制御を実行する | フェイルファースト方針によりFR-023も即時終了する | 異常終了・後続フェーズが実行されないことを確認 |
| `ST-FR-023-04`（北方領土除外メッシュ） 日本全土1次メッシュコードリスト（北方領土除外メッシュを含む）で全国実行する | パイプライン制御を実行する | 北方領土除外メッシュを省略したままフェーズ2が完走し、後続フェーズへ移行する | フェーズ2がエラー終了せず完走すること、除外メッシュの省略ログが記録されることを確認 |

### 3.4 地形解析エンジン

#### FR-004: 3×3メッシュ結合解析オーケストレーション

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-004-01` 対象メッシュの3×3範囲のローカルキャッシュタイルが存在 | 3×3結合解析を実行する | 解析範囲標高グリッド生成→[FR-015](20_SRS.md#fr-015-標高地形図出力)→[FR-005](20_SRS.md#fr-005-ピーク候補検出)/[FR-006](20_SRS.md#fr-006-コル検出プロミネンス計算)→[FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用)→[FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成)→meshset書き出しの順に処理される | 各段階の出力ファイル（標高地形図PNG・per-mesh CSV・per-mesh GeoJSON・meshset）の生成順序をログで確認 |
| `ST-FR-004-02`（異常系） 北方領土除外メッシュ以外の対象メッシュのローカルキャッシュタイルが1枚も存在しない（地理院側未整備による全欠如を含む） | 3×3結合解析を実行する | エラーを出力して処理を中止する | 異常終了・エラーメッセージを確認 |
| `ST-FR-004-03`（北方領土除外メッシュ） 日本全土1次メッシュコードリストに北方領土除外メッシュを含む状態で1次メッシュコードリストを省略して実行する | 3×3結合解析を実行する | 除外メッシュは対象メッシュ（解析中心）として選ばれず、per-mesh CSV・GeoJSON・標高地形図・解析識別子・meshsetを生成しない。対象メッシュとして選ばれなかったことが実行ログに記録される | 除外メッシュを解析中心とするper-mesh成果物が存在しないこと、省略ログの記録を確認 |
| `ST-FR-004-04`（北方領土除外メッシュ・窓内） 隣接する対象メッシュ（非除外）の3×3窓内に北方領土除外メッシュリストのメッシュを含む | 3×3結合解析を実行する | 除外メッシュを窓内でNODATA充填して解析が完走する。除外メッシュはbounding box・解析対象メッシュ範囲情報・meshsetに含まれない | 解析が正常完了すること、除外メッシュが出力の解析対象メッシュ集合・meshsetに含まれないことを確認 |
| `ST-FR-004-05`（利用者指定リスト） 利用者指定の1次メッシュコードリストに北方領土除外メッシュのコードを含めて実行する | 3×3結合解析を実行する | 当該コードが警告ログ付きでスキップされ、残りのメッシュコードについて処理を継続する | 警告ログの出力・処理継続を確認 |

#### FR-014: 広域結合解析オーケストレーション

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-014-01` [FR-022](20_SRS.md#fr-022-コル充足判定)がkey_col_resolved=falseピークを検出、N=4で呼び出し | 広域結合解析を実行する | N×N（N=4）メッシュの4コーナーパターン（NW/NE/SW/SE）で解析が実行される | 4コーナー分の広域per-mesh CSV・meshsetが生成されることを確認 |
| `ST-FR-014-02`（異常系） 北方領土除外メッシュ以外のウィンドウ範囲の存在メッシュにローカルタイルが1枚も存在しない（地理院側未整備による全欠如を含む） | 広域結合解析を実行する | エラーを出力して処理を中止する | 異常終了・該当メッシュコードを含むエラーメッセージを確認 |
| `ST-FR-014-03`（境界値） 4コーナー全パターンを試行してもkey_col_resolvedが確定しないピーク | 広域結合解析を実行する | 当該ピークはkey_col_resolved=falseのまま各コーナーパターンの広域per-mesh CSVに出力される | 出力CSVでkey_col_resolved=falseを確認 |
| `ST-FR-014-04`（北方領土除外メッシュ） ウィンドウ範囲に北方領土除外メッシュリストのメッシュを含むコーナーパターン | 広域結合解析を実行する | 除外メッシュをNODATA充填し、当該コーナーパターンが停止せず完走する | 異常終了しないこと、除外メッシュ以外の対象ピークが処理されることを確認 |
| `ST-FR-014-05`（境界値） 同じ 2×2 画素内で、DEM5a が 1 画素だけ低い有効値・残り 3 画素が NODATA、その 3 画素を DEM5b または DEM10b がより高い値で補う | 広域結合解析を実行する | ズームレベル14 の当該画素の標高が、各 Z15 画素でフォールバック・デコードした 4 値の最大値（下位 DEM が補った高い値）になる | 解析範囲標高グリッドの当該画素値を照合（[ADR-SRS-055](decisions/ADR-SRS-055-wide-analysis-pooling-after-fallback.md)） |
| `ST-FR-014-06`（境界値） DEM10b だけで覆われた地点に独立峰があり、頂上の DEM10b 画素が Z15 の 2×2 へ同じ標高で複製される | 通常解析（N=3）でコル未確定となったあと、広域結合解析を実行する | 広域のピークは 2×2 の北西の画素のズームレベル15 座標で出力され、通常解析のピーク座標と一致する | 広域 per-mesh CSV に対象ピークの行が残り、統合後の `merged_peak.csv` で当該ピークが 1 行・`key_col_resolved=true` になることを確認 |

#### FR-002: DEM 階層フォールバック

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-002-01` DEM5aが有効値のピクセル | DEMフォールバック処理を実行する | DEM5aが採用される | 採用DEM階層を確認 |
| `ST-FR-002-02`（境界値） DEM5a/5b/5cが全てNODATA、DEM10bのみ有効値のピクセル | DEMフォールバック処理を実行する | DEM10bへフォールバックする | 採用DEM階層を確認 |
| `ST-FR-002-03`（境界値） 全DEM階層がNODATAのピクセル | DEMフォールバック処理を実行する | RGB(128,0,0)を返す（正常分岐） | 出力RGB値を確認 |

#### FR-003: 標高デコード・NODATA 処理

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-003-01` 通常の正標高RGB値（例: 標高500m相当） | 標高デコードを実行する | `標高 = (R×65536+G×256+B)/100.0` の計算式通りにデコードされる | デコード結果を数値照合 |
| `ST-FR-003-02`（境界値） NODATAピクセル（R=128, G=0, B=0） | 標高デコードを実行する | -9999mとしてデコードされる | デコード結果が-9999であることを確認 |
| `ST-FR-003-03`（境界値） 海面下ピクセル（中間値x = 0x800001以上、符号変換対象） | 標高デコードを実行する | `x - 2^24` を適用した負の標高値としてデコードされ、後段のタイル処理で海面（0m）に統一される | デコード直後の負値・後段統一後の0mを両方確認 |
| `ST-FR-003-04`（境界値） NODATA判定の境界値（x = 0x800000ちょうど） | 標高デコードを実行する | NODATA（-9999m）として判定される（符号変換より先にNODATA判定が適用される） | デコード結果が-9999であることを確認 |

#### FR-015: 標高地形図出力

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-015-01` 解析範囲標高グリッドに陸地画素が存在 | 標高地形図出力を実行する | gist_earth+陰影起伏方式（[ADR-SRS-047](decisions/ADR-SRS-047-terrain-color-scheme-gist-earth-hillshade.md)）でPNGが生成される | 出力PNGの存在・長辺6000px縮小を確認 |
| `ST-FR-015-02`（境界値） 標高0m以下（海面・NODATA統一後）の画素 | 標高地形図出力を実行する | 白（`#FFFFFF`）でマスクされる | 該当画素の色を確認 |
| `ST-FR-015-03`（異常系） 解析範囲標高グリッドに陸地画素（標高>0m）が1つも存在しない（`vmax<=0`） | 標高地形図出力を実行する | 異常終了する（2026-07-12追加。[FR-015異常系](20_SRS.md#fr-015-標高地形図出力)参照） | 異常終了を確認 |

#### FR-005: ピーク候補検出

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-005-01` 解析範囲標高グリッド（既知のピーク地形を含む） | ピーク候補検出を実行する | 局所的な地表面最高点がピーク候補として検出される | 検出されたピーク候補座標を既知の地形と照合 |
| `ST-FR-005-02`（境界値） 同標高の隣接ピクセルが複数存在する状況 | ピーク候補検出を実行する | タイブレーク規則（ピクセルインデックス昇順）で1つが候補となる | 決定論的に同一結果が得られることを再実行で確認（[NFR-003](20_SRS.md#nfr-003-再現性決定論的出力)） |

#### FR-006: コル検出・プロミネンス計算

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-006-01` 2つの連結成分が接触するピクセルを含む解析範囲 | コル検出を実行する | 接触ピクセルがコルとして検出され、プロミネンス=ピーク標高-コル標高が算出される | コル座標・プロミネンス値を照合 |
| `ST-FR-006-02`（境界値） 陸地連結成分が外周1px海面ボーダーに接触する状況 | コル検出を実行する | `key_col_resolved=false`、`col_elev`/`prominence`は空欄、`col_lat`/`col_lon`=0.0 | 出力sentinel値の組み合わせを確認 |
| `ST-FR-006-03`（境界値） 走査ループ完了後、外周1px海面ボーダーへの接触フラグが立っていない連結成分（島の最高峰） | コル検出を実行する | 海面確定規則によりKeyコル=海面(0m)・`key_col_resolved=true`、`col_lat`/`col_lon`=0.0 | 出力sentinel値の組み合わせ（`key_col_resolved=true` + 座標0.0）を確認 |
| `ST-FR-006-04`（境界値） ピーク標高が同じ 2 つの連結成分が接触する解析範囲 | コル検出を実行する | ソート順で先に処理された頂点を高い方とし、もう一方のピークの Key コルとして接触ピクセルを確定する | 入力の並びを変えずに複数回実行し、Key コルの割当が常に同じことを確認 |

#### FR-007: per-mesh CSV 出力（プロミネンス閾値適用）

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-007-01` `key_col_resolved=true`、プロミネンス一次フィルタ閾値以上のピーク | per-mesh CSV出力を実行する | CSVに出力される | 出力CSVにレコード存在を確認 |
| `ST-FR-007-02`（境界値） `key_col_resolved=true`、プロミネンスが一次フィルタ閾値ちょうど | per-mesh CSV出力を実行する | 「以上」条件のため出力される | 出力CSVにレコード存在を確認 |
| `ST-FR-007-03`（境界値） `key_col_resolved=false`（プロミネンス未確定）のピーク | per-mesh CSV出力を実行する | フィルタ適用されず無条件に出力される | 出力CSVにレコード存在を確認 |
| `ST-FR-007-04`（境界値） 解析対象メッシュ範囲外（フリンジ部分）で検出されたピーク | per-mesh CSV出力を実行する | 地理的範囲フィルタにより出力対象外となる | 出力CSVに該当レコードが存在しないことを確認 |

#### FR-016: ピーク域ポリゴン生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-016-01` フィルタ後ピーク候補・コル情報リスト（解析範囲内で完結） | ピーク域ポリゴン生成を実行する | `area_complete=true` のアクティベーションゾーン・delete判定ゾーンポリゴンが生成される | 出力GeoJSONのポリゴン・`area_complete`値を確認 |
| `ST-FR-016-02`（境界値） ポリゴンが解析範囲外で途切れる状況 | ピーク域ポリゴン生成を実行する | `area_complete=false` として出力される（正常な状態フラグ） | 出力GeoJSONの`area_complete=false`を確認 |

### 3.5 統合・突合コンポーネント

#### FR-008: per-mesh CSV 統合

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-008-01` 複数のper-mesh CSVに同一ピーク座標が重複出現 | per-mesh CSV統合を実行する | 代表採用ロジック（key_col_resolved=true優先→col_elev最高優先→通常モード優先→analysis_id昇順）で1件に統合される | 統合結果のレコードが代表採用ロジック通りであることを確認 |
| `ST-FR-008-02`（境界値） 通常モードで`key_col_resolved=false`、広域モードで`key_col_resolved=true`の同一ピーク | per-mesh CSV統合を実行する | 広域モード結果が代表として採用される | 統合結果の`key_col_resolved`・`analysis_id`を確認 |
| `ST-FR-008-03`（境界値、issue #16関連） 通常モード行、`analysis_count ≠ expected_count`の状況 | per-mesh CSV統合を実行する | `stability=unstable`と判定される | 出力CSVの`stability`列を確認 |
| `ST-FR-008-04`（境界値） 陸地最高峰リスト対象ピーク（富士山等）で自動走査は`key_col_resolved=false` | per-mesh CSV統合を実行する | `key_col_resolved=true`（Keyコル=海面0m）に更新されるが`stability`は`unstable`のまま維持される | 出力CSVの`key_col_resolved`と`stability`の組み合わせを確認 |
| `ST-FR-008-05`（異常系） per-mesh ピーク候補CSVが1件も存在しない | per-mesh CSV統合を実行する | エラー終了する | 異常終了を確認 |
| `ST-FR-008-06`（北方領土除外メッシュ） 北方領土除外メッシュに隣接するピーク（通常モード行）の`expected_count`を算出する | per-mesh CSV統合を実行する | 周辺メッシュのうち北方領土除外メッシュに該当するものは`expected_count`の母数から除かれる | 出力CSVの`expected_count`が除外メッシュを除いた期待値と一致することを確認 |
| `ST-FR-008-07`（不変条件） 窓に北方領土除外メッシュを含む3×3結合解析（[FR-004](20_SRS.md#fr-004-33メッシュ結合解析オーケストレーション)）を実行したper-mesh ピーク候補CSV・GeoJSON | per-mesh CSV統合を実行する | 除外メッシュが解析対象メッシュにならず[FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用)の地理的範囲フィルタで出力されないため、per-mesh ピーク候補CSVの時点で北方領土6村ポリゴン内のピーク座標を含む行が存在せず、統合後の出力にも含まれない | per-mesh ピーク候補CSVと統合後の出力CSVの双方に北方領土6村ポリゴン内のピーク座標が存在しないことを確認 |
| `ST-FR-008-08`（境界値） `key_col_resolved=true`、プロミネンスがプロミネンス最終フィルタ閾値ちょうどのピーク | per-mesh CSV統合を実行する | 「以上」条件のため統合後の出力に残る | 出力CSVにレコード存在を確認 |
| `ST-FR-008-09`（境界値） 代表採用後も`key_col_resolved=false`（プロミネンス未確定）のピーク | per-mesh CSV統合を実行する | 最終フィルタが適用されず統合後の出力に残る | 出力CSVにレコード存在（`prominence`空欄）を確認 |
| `ST-FR-008-10`（境界値） 代表レコードが広域モードで `key_col_resolved=true`、プロミネンスが一次フィルタ閾値以上・最終フィルタ閾値未満のピークと、同条件の通常モード代表ピーク | per-mesh CSV 統合を実行する | 両ピークとも `merged_peak.csv` から除外され、広域モード代表のピークだけが警告ログの除外一覧に座標・標高・コル標高・プロミネンス・`analysis_id` 付きで出る | ログの一覧と `merged_peak.csv` の内容を確認（[ADR-SRS-056](decisions/ADR-SRS-056-wide-analysis-prominence-underestimate-warning.md)） |
| `ST-FR-008-11`（境界値） 同じピーク座標の通常モード 2 件と広域モード 1 件のレコードがすべて `key_col_resolved=false` で、入力ファイルの読み込み順を入れ替えた 2 通り | per-mesh CSV 統合を実行する | どちらの順でも代表は通常モードのうち `analysis_id` が最小のレコードになり、`analysis_count` は通常モード行だけを数えた 2 になる | 2 回の `merged_peak.csv` が一致し、`analysis_count=2` を確認 |

#### FR-018: per-mesh ピーク候補 GeoJSON 統合

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-018-01` 同一ピーク座標で`area_complete=true`のポリゴンが存在 | GeoJSON統合を実行する | `area_complete=true`のポリゴンが採用される | 出力GeoJSONを確認 |
| `ST-FR-018-02`（境界値） `area_complete=true`のポリゴンが見つからないピーク | GeoJSON統合を実行する | `area_complete=false`のポリゴンがフォールバックとして採用される | 出力GeoJSONを確認 |

#### FR-022: コル充足判定

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-022-01` `merged_peak.csv`に未確定ピーク（`key_col_resolved=false`）が存在 | コル充足判定を実行する | 未確定有無「残あり」を返し、`key_col_unresolved_peaks-<N>.csv`が出力される | 戻り値・出力ファイルを確認 |
| `ST-FR-022-02` `merged_peak.csv`の全ピークが`key_col_resolved=true` | コル充足判定を実行する | 未確定有無「ゼロ」を返す | 戻り値を確認 |
| `ST-FR-022-03`（境界値） `merged_peak.csv`がピーク件数ゼロ（空CSV） | コル充足判定を実行する | 未確定有無「ゼロ」を返して正常終了する | 戻り値を確認（エラーにならないこと） |
| `ST-FR-022-04`（異常系） `merged_peak.csv`が存在しない | コル充足判定を実行する | エラー終了する | 異常終了を確認 |

#### FR-009: SOTAリスト突合・match_status 判定

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-009-01`（境界値） ピークの AZ 内に現役 SOTA 登録がちょうど 1 件存在 | SOTAリスト突合を実行する | `peak.match_status="matched"` | 出力GeoJSONのmatch_statusを確認 |
| `ST-FR-009-02`（境界値） AZ 内登録 0 件のピークの delete 判定ゾーン内に、全ピークの AZ 外である現役サミットが存在 | SOTAリスト突合を実行する | `peak.match_status="dominant"`、当該サミットが削除候補 | 出力GeoJSONのmatch_status・delete候補を確認 |
| `ST-FR-009-03`（境界値） delete判定ゾーン内にも既存サミットが存在しないピーク | SOTAリスト突合を実行する | `peak.match_status="new"` | 出力GeoJSONのmatch_statusを確認 |
| `ST-FR-009-04`（境界値） 既存サミット座標がいずれのAZ・delete判定ゾーンにも含まれない | SOTAリスト突合を実行する | `summit.match_status="unmatched"`（要確認）、停止せず続行 | 出力GeoJSONのmatch_statusを確認、処理継続を確認 |
| `ST-FR-009-05`（異常系・不備ゲート1） `unmatched`件数が要確認サミット件数しきい値を超える | SOTAリスト突合を実行する | 不備ゲートとして異常終了する（GeoJSON/XLSXは出力してから停止） | 異常終了・出力ファイル存在の両方を確認 |
| `ST-FR-009-06`（異常系・不備ゲート2） AZまたはdelete判定ゾーンポリゴンの`area_complete=false`が1件以上 | SOTAリスト突合を実行する | 不備ゲートとして異常終了する | 異常終了・出力ファイル存在を確認 |
| `ST-FR-009-07`（異常系・不備ゲート3） `key_col_resolved=false`のピークが1件以上（N=6エスカレーション後も未解消） | SOTAリスト突合を実行する | 不備ゲートとして異常終了する | 異常終了・出力ファイル存在を確認 |
| `ST-FR-009-08`（境界値） 仮サミットコード採番: 同一エリアで標高・プロミネンス・座標が異なる複数ピーク | SOTAリスト突合を実行する | 標高降順→プロミネンス降順→peak_lat降順→peak_lon昇順の順序で`A00`から採番される | 採番結果の順序を照合 |
| `ST-FR-009-09`（境界値） 同一エリア内で仮サミットコード連番上限（prefix `99`）を超過 | SOTAリスト突合を実行する | エラーメッセージ（地域コード・超過件数を含む）を出力して異常終了する | 異常終了・エラーメッセージ内容を確認 |
| `ST-FR-009-10` 同一 AZ 内に現役 2 件（登録標高が同値 / 異なる場合） | 各入力で突合・申請出力を行う | ピークと2登録が ambiguous/review。コード選択・仮コード採番なし。一覧2行、申請0行 | GeoJSON の理由・組 ID・空コード・null フラグ、両 XLSX と申請行を照合 |
| `ST-FR-009-11` 同一 AZ 内に現役 3 件 | 突合・申請出力を行う | 全件 ambiguous/review。一覧3行、申請0行 | 3コードを保持しピークの架空行がないことを確認 |
| `ST-FR-009-12` 異なる2コードの登録座標が完全一致 | 突合し各コードを検索する | 別登録 Point・一覧2行を保持し、検索で双方に到達 | コードごとの Point・行・popup を確認。重なる線による登録欠落なし |
| `ST-FR-009-13` 同一 AZ 内に現役1件・廃止済み1件 | 突合する | 現役1件として通常 matched、保留組なし | ValidTo 絞り込みと category を確認 |
| `ST-FR-009-14` 複数登録ピークが AZ 外削除候補の主ピークに選ばれる | 突合・申請出力を行う | 外の候補は delete/review、理由 ambiguous_parent。距離と同じ組 ID を保持しコード・rationale は空文字、削除申請0行 | AZ 内2件・外1件の一覧3行、全接続線 review、外の解析属性は空欄を確認 |
| `ST-FR-009-15` AZ 外削除候補が通常ピークと ambiguous ピーク双方の delete 判定ゾーン内 | プロミネンス・座標の選択結果が各親になる入力で突合する（同値と null の比較も含む） | 従来の主ピーク選択を先に適用。選ばれた親が ambiguous の場合だけ保留、別候補へ付け替えなし | 選ばれた親・category・距離・組 ID を照合。重なりによる保留の連鎖なし |
| `ST-FR-009-16` 通常 matched ピークの AZ 内1件・AZ 外削除候補1件 | 突合・申請出力を行う | 通常の delete 行と既存親コードによる削除申請を維持 | AZ 内はバンド遷移有無に応じた category、AZ 外は delete、申請削除1行を確認 |
| `ST-FR-009-17` 複数登録が多数で、孤立 unmatched はしきい値以下、他の不備なし | 突合を実行する | 続行。複数登録は孤立件数に加算しない。組数・AZ 内件数・AZ 外保留件数を別々に警告記録 | 終了状態、組座標・コード一覧と GeoJSON・XLSX の同じ組の対応を確認 |
| `ST-FR-009-18` 保留ピークの AZ または delete 判定ゾーンが不完結、あるいはコル未確定 | 各不備を個別に与えフェーズ4を実行する | review でも全件評価後に GeoJSON/XLSX を出力して停止し、ビューア生成をスキップ | 不備フラグ・出力を確認。コル未確定時は col と peak_col_link を生成しない |
| `ST-FR-009-19` 孤立 unmatched がしきい値を超過し、複数登録は有 / 無 | 両条件でフェーズ4を実行する | 既存ゲートで停止、GeoJSON/XLSX は出力、ビューア生成なし | 孤立件数のみによる停止と保留組の保持を確認 |
| `ST-FR-009-20` | 前回の担当者指定削除をブラウザに保存、今回は unmatched 件数超過・ポリゴン不完結・コル未確定の各不備入力 | 各入力でフェーズ4を実行し、保存値なしの場合とも比較する | 既存ゲートで同じく停止し、中心 GeoJSON/XLSX を出力、ビューア生成をスキップ | 過去判断がバッチ件数・不備を隠さない。全 GeoJSON フィーチャ・一覧の review_decision/review_note は空文字 |
| `ST-FR-009-21` | 大量の除外を保存、unmatched 件数超過・ポリゴン不完結・コル未確定の各不備入力 | 台帳あり/なしでバッチを比較 | 出力・停止・ビューア生成スキップは同じ。全中心フィーチャ/バッチ一覧の除外属性は空文字 | 判断ファイルはバッチ入力にせず不備を隠さない |
| `ST-FR-009-22`（境界値） CSV 1行目の基準日と同日・前日・翌日の `ValidTo` を持つ登録、および基準日を解釈できない 1行目 | 同じ CSV で実行日を変えて突合を実行する | `ValidTo` が基準日と同日・翌日の登録は現役として突合し、前日の登録は廃止済みとして除外する。実行日を変えても結果は同一。基準日を解釈できない場合はエラー終了する | 基準日との比較だけで絞り込まれ、実行日に依存しないことを確認（[ADR-SRS-054](decisions/ADR-SRS-054-summitslist-validto-reference-date.md)） |

### 3.6 可視化生成コンポーネント

#### FR-013: HTML ビューア生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-013-01` `merged_summit.geojson`が正常生成済み | HTMLビューア生成を実行する | `merged_viewer.html`と`merged_viewer_data.js`が生成され、データファイルにGeoJSONの内容が格納される | 2ファイルの存在とデータファイルの内容を確認 |
| `ST-FR-013-02`（異常系） [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定)が不備ゲートとして異常終了した状態 | HTMLビューア生成を実行する | 実行をスキップしHTML・データファイルとも生成しない | HTML・データファイル未生成を確認 |
| `ST-FR-013-03`（異常系） `merged_summit.geojson` が存在しない状態でフェーズ4 のビューア生成だけを起動する | HTMLビューア生成を実行する | エラーを出力して終了し、HTML・データファイルを生成しない | 異常終了と出力ファイルが無いことを確認 |

### 3.7 申請書生成 UI

#### FR-019: HTML ビューア機能仕様

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-019-01` `merged_viewer.html`をブラウザで開く | 地図表示・マーカー・背景タイル切替を操作する | Leaflet地図が表示され、feature_typeに応じた形状（summit=●/peak=▲/key_col=▼）でマーカーが描画され、`© 国土地理院`（リンク付き）と「加工して作成」の旨が常時表示される | Claude in Chromeによる目視確認 |
| `ST-FR-019-02` `category=add`のピーク（`match_status`が`new`と`dominant`の各1件）を選択 | 山岳名JP/EN入力フィールドに入力する | 入力内容がlocalStorageに保存される | 再読み込み後も入力内容が保持されていることを確認 |
| `ST-FR-019-03` 検索ボックスにサミットコード・山岳名の一部を入力 | サジェスト候補から選択する | 該当サミットへ地図ズーム移動しポップアップが開く | Claude in Chromeによる目視確認 |
| `ST-FR-019-04`（境界値） 新しいパイプライン実行で`generated_at`が変わった状態でビューアを開く | 前回入力内容の引き継ぎ確認ダイアログを操作する | 通常の山岳名・rationale は新しい`generated_at`キーに引き継がれる（担当者指定削除は `ST-FR-019-10` の別規則） | localStorageの内容を確認 |
| `ST-FR-019-05` 独立した保留組2組（各組にコル・AZ 内登録・AZ 外保留登録あり） | 要確認フィルター、各登録のコード・名称検索、popup、ピーク↔コル移動を操作する | 全地形・登録・線が表示対象。「要確認ピーク」と座標、AZ 内外を区別した一覧、個別の理由・同じ組への参照を表示 | 空の親コードで混同せず review_group_id ごとに検索・移動が成立することを確認 |
| `ST-FR-019-06` 前回 matched 登録の山岳名・rationale 編集値が localStorage にあり、今回は保留組 | 再訪・引き継ぎを選び、popup と両エクスポートを確認する | 保留組に編集 UI・採用操作がなく、現行の登録値・要確認表示・category を上書きしない | 申請への漏出なし。改訂一覧・ZIP も現行 review データを保持 |
| `ST-FR-019-07` | 正式コードを持つ孤立 unmatched（review_reason=unmatched・組 ID 空） | 未操作状態を確認し、削除指定して前後空白と内部改行のある根拠を入力する | 初期は review・申請なし、選択後は delete/unmatched・review_decision=delete。本文の前後だけを除去 | 手動判断ラベル・専用根拠入力・読み取り専用 rationale、登録値不変、親・コル・線なしを確認 |
| `ST-FR-019-08` | 根拠を入力した担当者指定削除 | 要確認へ戻し、出力・再訪後に再選択する | 取消後は review、判断・本文・rationale は空。根拠下書きは再選択時に復元 | 取消時は申請行なし・review.geojson のみで下書きが成果物に漏れない |
| `ST-FR-019-09` | 同一 generated_at の有効な削除指定保存値と、通常 rationale だけの旧保存値 | ビューアを再読込する | 適格な明示判断だけ復元。旧 rationale だけでは採用しない | category・review_decision・review_note と元のバッチ入力不変を確認 |
| `ST-FR-019-10` | 前世代で削除指定、今回も同コードが適格 | 新しい generated_at で継承する場合としない場合を試す | 継承時は前回指定の表示と根拠下書きのみ、再選択まで review。非継承時は初期状態 | 再選択前の申請行なし・現在の判断/本文/rationale 空文字。両経路とも前回保存値を保持 |
| `ST-FR-019-11` | 前回の削除指定コードが今回不在・matched・廃止済み・保留組 AZ 内/外の各状態 | 継承して表示と出力を確認する | 対象外判断は適用せずコード通知、通常カテゴリ・登録値・保留組を保持 | 保留組に採用 UI なし。近接座標や別コードへ転用せず、前回保存値を保持 |
| `ST-FR-019-12` | 削除フィルター OFF の適格登録 | 指定後、要確認 OFF にして取消。コード・名称検索を行う | 移動先フィルターが ON となり同じ登録と popup を追える | 両方向の状態遷移・検索を目視確認し、親・コル移動ボタンがないことを確認 |
| `ST-FR-019-13` | 未知の判断値・型不正・対象外コードの保存レコード、有効な別レコードと通常編集 | 再訪して出力。その後保存失敗を模擬し有効な対象を操作・出力する | 不正レコードは無視してコード通知、他の有効編集は利用可能。保存失敗は未保存表示 | 正しいメモリ状態から出力でき、保存成功を偽装しないことを確認 |
| `ST-FR-019-14` | add(new/dominant)・通常 delete・band_change と関連フィーチャ | 除外・理由入力・解除を行い、除外を含む表示 OFF から検索・除外も行う | 行単位の manual/解除と関連属性が一致し、元の分類・登録/解析値は不変。対象と popup を保持 | 必要なフィルターを ON、明瞭な除外ラベル、理由前後空白除去・内部改行保持。no_change/review/保留組に操作なし |
| `ST-FR-019-15` | 除外理由が空白/改行だけの候補と理由入力済み候補 | 個別除外し、申請書/ZIP をそれぞれ出力 | 未記入も manual が有効、本文は空文字。未記入件数・コードを警告して出力 | manual/parent_add の行数別集計、未適用判断の通知、未記入表示。既存の不備/手動削除根拠警告も維持 |
| `ST-FR-019-16` | 親 dominant に従属 delete が複数、別親の子と重なるゾーンの候補あり | 親 add だけを除外する。parent_add 子から解除を試す | 親 manual、選択された親に従属する子だけ parent_add。派生の直接解除は提供せず親へ移動 | 親の今回仮コード入り固定理由、子の線も子の属性。parent_add は JSON に保存しない |
| `ST-FR-019-17` | 子1件 manual、親 manual、別の子は parent_add | 親の除外だけ解除 | 独自判断と理由は子に残り、parent_add の子だけ申請へ戻る | 操作順を入れ替えても状態・台帳が一致し、子の理由を親理由で失わない |
| `ST-FR-019-18` | 親 add と通常 delete の関係あり | 子の delete だけ除外して両申請出力 | 親 add を残し、存続予定登録と追加候補の併存をコード付きで警告して続行 | 申請書に親だけを残し、エビデンスには除外された子と関連線も保持 |
| `ST-FR-019-19` | matched 親が band_change、従属通常 delete あり | 親の band_change を除外 | 親の除外は子へ波及せず delete は申請対象のまま | 既存登録が存続することを表示・申請・エビデンスで照合 |
| `ST-FR-019-20` | 適用中と対象不在の manual 判断、判断時条件/理由/出自日時を含む台帳 | JSON 保存、ブラウザデータ消去または別 PC の同じ対象で読込、同世代へ再訪 | 有効な判断を再検証して復元し、未適用も保持して往復保存 | 座標数値精度・条件・理由・日時不変。JSON は独立出力で出典/加工明示を含み、通常編集や手動削除判断は含まない |
| `ST-FR-019-21` | 前世代で判断を保存、同条件の今回候補あり | 新しい generated_at で継承する/しない両経路を試す | 対象が除外台帳も含むと確認。継承時は一致判断を個別再選択なしで適用、非継承は空台帳 | 適用/未適用件数、前世代値保持、担当者指定削除は別規則の個別再採用 |
| `ST-FR-019-22` | 仮コードだけが再採番、座標と条件は同じ | 新世代へ判断を継承 | 同一対象として除外し、表示/parent_add 理由は今回の仮コード | 名称・rationale・基準日・ソフトウェア版だけの変更でも失効しない |
| `ST-FR-019-23` | 同じ仮コードが別座標へ再割当、または旧座標から近傍へピーク移動 | 台帳を継承/読込 | 旧記録は未適用のまま保持、現行候補は未除外と通知 | 丸め・距離許容・近傍転用なし。新しい座標は新規判断し旧記録は自動削除しない |
| `ST-FR-019-24` | 同じ正式コードでアクション/座標/標高/主ピークが変化、または add の従属削除集合が変化 | 台帳を継承し差異を確認、対象を再除外 | 前回判断は未適用、今回の申請可否と差異を通知。再除外で同じキーの条件を更新 | 各条件を一つずつ変え確認。数値/null の区別、コードの要素集合が同じなら順序差は不問、要素が変われば親 add も失効 |
| `ST-FR-019-25` | 対象が不在・廃止済み・no_change・保留組の各状態 | 継承後の一覧と出力を確認し、後に同条件で再出現させる | 未適用記録を保持し、現在の申請対象/要確認等を正しく併記。同条件再出現時は適用 | バッチ値・保留組不変、不在を架空フィーチャにしない。範囲縮小による不在も同様 |
| `ST-FR-019-26` | 担当者指定削除と同コードの通常 delete 用の過去除外記録 | 読込/継承し、手動削除の取消と新世代への移行を行う | 担当者指定削除に除外 UI なし。過去記録は適用せず、外すには「要確認に戻す」 | 台帳は手動削除を復活・除外させない。新世代の個別再採用と review_note 未記入時の両出力警告を維持 |
| `ST-FR-019-27` | 同じ永続キーが現行の複数候補を指す | 有効な1件の判断レコードを読込 | 全候補への適用も勝者選択もせず未適用通知 | 台帳記録は保持、現行候補状態は不変 |
| `ST-FR-019-28` | 現在の有効な台帳・表示・通常編集、不正な判断ファイル | 不正 JSON/非対応版/項目欠落/型不正/非有限値/座標範囲外/重複キーの各読込と読込取消を試す | ファイル全体を拒否または取消し、現状態を一切変えない | 内容同一の重複も拒否。理由文字列は HTML/コードとして実行せず URL も取得しない |
| `ST-FR-019-29` | 編集中台帳と、追加/変更/削除を含む別ファイル・空台帳・解除前の古いファイル | 差分プレビューで取消/確認を試し、同じファイルを再読込 | 確認後のみ全体置換。空台帳も有効、再読込で増殖せず自動マージなし | 適用/未適用件数、古い判断の復活をプレビュー。置換前に保存可能、通常編集・手動削除判断不変 |
| `ST-FR-019-30` | 保存不能なブラウザと有効なメモリ台帳、壊れたキャッシュ、ファイル書出後の編集 | 再訪・編集・JSON 保存・両申請出力を行う | 壊れたキャッシュは空台帳と明示しファイル復旧を案内。保存失敗は未保存、有効メモリから出力可能 | ブラウザ未保存/ファイル未反映を区別し、ダウンロード要求をディスク保存保証と表示しない。保存だけを出力/終了の必須条件にしない |
| `ST-FR-019-31` | 除外理由入力欄と今回適用する除外理由あり | 理由入力、申請書ボタン、ZIP ボタンを操作 | 理由が公開エビデンスに含まれることを入力欄と両ボタンで告知 | 除外理由が実際に改訂一覧と GeoJSON に入り、通常 rationale/review_note を上書きしない |
| `ST-FR-019-32` | 適用中の除外と解析範囲外の記録、除外理由の下書き | 一方を解除して JSON/ZIP を保存。不要な未適用記録を一覧から削除 | 解除した判断は JSON から消え、単に不在の判断は残る。明示削除した未適用記録は消える | 下書きは同世代ブラウザ内のみ、成果物へ漏れない。再除外は明示操作を要する |
| `ST-FR-019-33` | 除外台帳/属性のない旧保存値、判断ファイルなし | 再訪して出力 | 既存仕様と同じ申請・表示で勝手な除外なし | 初期台帳は空、除外属性は空文字 |
| `ST-FR-019-34` | 廃止済みの山が new として再検出される | 未操作時を確認、明示除外して次回同条件へ継承 | 履歴だけでは除外しない。一度判断した一致候補だけを申請から外す | 履歴近傍照合/表示/自動ルールなし。DEM 等で条件が変わると再確認 |
| `ST-FR-019-35`（境界値） 前世代で new ピーク 2 件に山岳名・rationale を入力済み。今回は同じエリアに標高の高い new ピークが 1 件増え、既存 2 件の仮コードが 1 つずつずれる。前世代の保存値には今回消えたピーク 1 件分も含む | 新しい generated_at で「引き継ぐ」を選ぶ | 既存 2 件には座標が一致する前世代の入力がそのまま付き、増えたピークは未入力。消えたピークの保存値は適用されず件数と座標が通知される | 申請書の追加行で山岳名と座標の対応を確認し、前世代の保存値が残っていることを localStorage で確認 |

#### FR-011: 申請書 XLSX 生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-011-01` `category=add`のピーク（山岳名JP入力済み） | 「申請書」ボタンをクリックする | アクション列=追加、B〜J列がテンプレート通りにマッピングされたXLSXがダウンロードされる | ダウンロードXLSXの列内容を照合 |
| `ST-FR-011-02`（境界値） `category=delete`・`category=band_change`のフィーチャが混在 | 「申請書」ボタンをクリックする | 各アクションに応じた行が出力され、`no_change`・`review`は出力されない | 出力XLSXの行数・アクション種別を確認 |
| `ST-FR-011-03` 保留2組（AZ 内合計4件・AZ 外保留1件）と通常 add / band_change / delete 各1件が混在 | 申請書をエクスポートする | 保留2組・5登録が申請対象外と警告し、通常3行のみ出力して続行 | match_status=delete/category=review も申請から除外。通常の列内容を維持 |
| `ST-FR-011-04` | 通常自動削除・担当者指定削除・未指定 review・保留組が混在 | 根拠を編集して申請書と ZIP を出力する | 申請は選択された手動削除と通常候補だけ。手動行 A=削除、B=正式コード、C/D/E=登録 JP/EN/標高、F/G/H/J 空 | 列 I と GeoJSON rationale 一致、一覧と GeoJSON review_note 一致。JP 未取得は C 空欄。通常自動削除の主ピーク・距離・根拠は保持 |
| `ST-FR-011-05` | 根拠未入力と空白/改行のみの担当者指定削除、根拠入力済みの対象 | 申請書・ZIP をそれぞれ出力する | 未記入の件数・コードを警告して全対象を出力。行を落とさない | review_note は空文字、rationale は「未記入（担当者補記）」を含む。入力済み対象を未記入件数に数えない |
| `ST-FR-011-06` | manual/parent_add・未除外候補・担当者指定削除が混在し、new の名称空欄もある | 申請書を出力し除外前と比較 | add/delete/band_change かつ除外空文字の行だけ。残る行の基準順と A〜J は不変 | new 名称警告は今回出力行だけを数える。除外状態を台帳から出力側で再計算しない |
| `ST-FR-011-07` | dominant ピーク 1 件と、それに従属する通常 delete 1 件、matched ピークに従属する通常 delete 1 件 | 申請書を出力する | dominant に従属する削除行の列 I は「追加申請するピーク（{親の座標}）に従属」で仮コードを含まず、座標が追加行の列 I の座標と一致する。matched に従属する削除行は親の正式コードを示す | 申請書だけで削除行と追加行を座標で突き合わせられることを確認 |

#### FR-012: サミット一覧（申請内容反映版）生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-012-01` 山岳名編集済みのビューア状態 | 「申請エビデンス」ボタンをクリックする（FR-012はインライン実行） | `merged_summit_revised.xlsx`が編集後の山岳名を反映して生成され、最後に「出典」シート（地理院タイルを加工して作成した旨と出典リンク）を持つ | ZIP内XLSXの内容と「出典」シートを確認 |
| `ST-FR-012-02` AZ 内2登録＋AZ 外保留1登録、孤立 unmatched1登録を含む中心 GeoJSON | バッチ一覧と ZIP 内改訂一覧を比較する | 保留3行＋孤立1行。AZ 内2行はピーク・コル・解析属性・AZ 完結フラグを反復、外と孤立は解析属性空欄 | 各行のコード・名称・地域・sota_* は自身の登録値、確認属性は summit Point の値。外の主ピークコードは空・距離保持、同じ組の AZ 内行から主ピークを確認。全 review のバンドフラグ空セル、架空ピーク行なし |
| `ST-FR-012-03` | 適格な孤立 unmatched を1件指定し根拠入力 | バッチ一覧と改訂一覧を比較し、取消後も出力する | 元の1行を同じ位置で delete/unmatched に更新。reason=unmatched・group 空、decision=delete・note=本文。取消後は review・decision/note 空 | 登録コード・名称・地域・sota_* を保持。peak/col 座標標高、prominence、points、stability、key_col_resolved、col_margin_px、analysis_count、expected_count、area_complete、バンドフラグ、dominant_* は全て空欄。バッチの2新列は全件空、rationale 列なし |
| `ST-FR-012-04` | manual/parent_add と未除外の全カテゴリ、関連フィーチャあり | バッチ一覧と改訂一覧を比較し除外解除後も出力 | 行数/位置/分類/登録/解析値を保持し、除外状態と理由の2列を転記 | バッチの2列は空文字、改訂は行の peak/summit と一致。下書きや不在対象の行を作らない |
| `ST-FR-012-05` | new ピークに山岳名JP/EN を入力、dominant ピークは JP のみ入力 | 「申請エビデンス」ボタンで ZIP を出力する | 改訂一覧の add 行の `summit_name` に入力した EN、EN 未入力の行は空文字。申請書の列 G と ZIP 内 GeoJSON の値と一致する | 3 成果物の名称を突き合わせる |

#### FR-021: 申請エビデンス ZIP 生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-021-01` `merged_summit.geojson` が5カテゴリ全てを含み、review に孤立 unmatched と複数登録の保留組が混在 | 「申請エビデンス」ボタンをクリックする | ZIPに`merged_summit_revised.xlsx`と5つのカテゴリ別GeoJSON（add/delete/band_change/no_change/review）が同梱され、各GeoJSONの`metadata`に`attribution`・`source_url`・`license_url`が入っている | ダウンロード ZIP と各 GeoJSON の metadata を確認。review に孤立・保留組の全件があり、保留接続線が delete 側に漏れないことも確認 |
| `ST-FR-021-02`（境界値） あるカテゴリのフィーチャが0件 | 「申請エビデンス」ボタンをクリックする | 該当カテゴリのGeoJSONも空FeatureCollectionとしてZIPに同梱される | ZIP内の該当ファイルが空FeatureCollectionであることを確認 |
| `ST-FR-021-03` 保留組2組・孤立 unmatched・通常候補が混在 | エビデンス ZIP を生成する | review.geojson に保留組全体と孤立登録、各他ファイルに通常 category のみを出力 | 組 ID によるピーク・コル・地形・登録・両種の接続線の対応、未生成コル等の除外、他 category への漏出・重複なしを確認 |
| `ST-FR-021-04` | 通常削除・選択中の孤立登録・下書きのある未選択/取消済み登録・保留組 | エビデンス ZIP を出力する | 手動削除 Point は delete.geojson のみ、未選択/取消後は review.geojson のみ、保留組全体も review のみ | 全5ファイルと空集合・metadata を保持。新属性と専用 rationale が一致、重複・下書き漏れ・架空の親/線なし。通常削除の親関連は保持 |
| `ST-FR-021-05` | 除外済み add/band_change/delete と関連フィーチャ、不在の台帳記録あり | ZIP を生成 | 5 GeoJSON と改訂一覧に全候補を保持し、除外属性・理由が現在状態と一致 | category ごとの相対順・metadata 維持。第6カテゴリ、JSON 同梱、架空/重複フィーチャ、下書き/不在記録の漏出なし |

#### FR-020: 公開用ビューア配信

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-020-01` 公開用データ置き場に申請エビデンスGeoJSON5本を配置し、静的ホスティングが配信済み | 公開用ビューアのURLを開く | 編集UI・エクスポート・localStorageを除く閲覧機能で5本の内容が表示され、`© 国土地理院`（リンク付き）と「加工して作成」の旨が表示され、配信されるGeoJSONが配置に用いたZIPの同梱物と同一 | 配信ファイルと配置に用いたZIP同梱物の差分比較（`metadata.attribution`を含む） + Claude in Chromeによる目視確認 |
| `ST-FR-020-02` リリース版と開発版の両方を配信済み | 両方のURLを開く | 両方が同時に閲覧でき、画面上でどちらかを判別できる | Claude in Chromeによる目視確認 |
| `ST-FR-020-03`（異常系） 公開用データ5本のうち1本が取得できない状態で配信 | 公開用ビューアのURLを開く | 残り4本の内容が表示され、欠落した1本を利用者が判別できる | Claude in Chromeによる目視確認 |
| `ST-FR-020-04`（境界値） `metadata.generated_at`が異なるZIPに由来する5本を混在配置して配信 | 公開用ビューアのURLを開く | 異なる解析結果の混在として利用者に提示される | Claude in Chromeによる目視確認 |
| `ST-FR-020-05` 保留組2組を含む同一 ZIP の GeoJSON 5本を公開配置 | 公開用で要確認フィルター・登録検索・popup・ピーク↔コル移動を行う | 作業用と同じ保留組の閲覧が成立し、編集・エクスポートはない | `ST-FR-019-05` と同じ組・一覧・検索・移動を確認 |
| `ST-FR-020-06` | 担当者指定削除を含む同一 ZIP の5本を公開配置 | 削除フィルター・検索・popup を確認する | 親なしの delete/unmatched Point を正常表示し、手動判断と根拠・未記入状態が確認できる | 親なしをエラー扱いせず、採用/取消/編集/出力 UI と localStorage 使用なし |
| `ST-FR-020-07` | manual/parent_add と理由空欄を含む同一 ZIP の公開データ | 除外表示切替・検索・popup・親への移動を操作 | 元のカテゴリと除外理由/未記入・関連親を閲覧可能。初期は除外も表示、検索で必要フィルター ON | 除外/解除/編集・台帳一覧/読込/保存・localStorage 使用なし。配信属性を再計算しない |

### 3.8 非機能要件

| ID | 対象NFR | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|---|
| `ST-NFR-001-01` [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | 統合後のピークデータ | `key_col_resolved=true` の全出力ピークのプロミネンスを検査する | 全てプロミネンス最終フィルタ閾値以上（プロミネンス未確定のピークは `ST-FR-008-09` で扱う） | `merged_peak.csv`を走査し`key_col_resolved=true`で閾値未満のレコードが無いことを確認 |
| `ST-NFR-001-02` | [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | 事前固定する基準セットと独立した根拠資料 | [基準セットと判定手順](#382-検出網羅性の基準セットと判定手順)の必須記録・代表区分・適格性・事前固定と承認を検査 | 非空の必須セットと全必須区分を充足、許容位置と入力版・設定が確定 | 製品の全版出力を正解根拠にせず、開始前の内容識別子・開始記録・終了時照合を確認。未確定や空区分は合格にしない |
| `ST-NFR-001-03` | [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | 独立導出して固定した期待メッシュ集合・省略集合（北方領土除外メッシュ）、製品リストと全国実行のログ/成果物 | 期待集合対製品リスト、期待集合対（正常完了集合 ∪ 省略集合）を別照合し、正常完了集合と省略集合が重ならないこと・製品の省略ログが独立導出した省略集合と一致することを確認。必要な広域段階も確認 | 欠落・重複なし、正常完了メッシュには per-mesh 成果物、省略メッシュには省略ログがあり、省略段階には既存仕様上の根拠あり | 件数一致だけで合格にせず、竹島/北方領土の除外粒度を区別。北方領土除外メッシュは期待集合から削除しない。入力欠損・異常終了（未整備による全欠如を含む）で分母を減らさない |
| `ST-NFR-001-04` | [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | 妥当性確認済みの基準と通常/必要な広域を含む全国最終 `merged_peak.csv` | 全必須レコードを許容位置で一括照合し、各区分も集計 | 一意の出力行、重複割当なし、コル確定かつ最終閾値以上を全件満たす | 漏れ0件・再現率100%。未確定/曖昧は成功に数えず、stability の `-`/`unstable` だけでは失敗にしない。突合や申請判断は未適用 |
| `ST-NFR-001-05` | [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | 下記の負例と正常対照を試験用に用意し、実出力と区別 | 照合・集計・合否報告の手順またはツールへ各例を渡す | 各例が下表どおり判定され、事後操作で成功へ変わらない | 100件中99件、重複/未確定、空区分等を誤合格にしない。人工例の成功で全国実測の合格を代用しない |
| `ST-NFR-002-01` [NFR-002](20_SRS.md#nfr-002-メモリ使用量) | 9メッシュ最大解析を実行 | メモリ使用量を監視しながら解析を実行する | 動作前提マシン（62.72GiB+スワップ48GiB）で完走する | 解析完了・OOM未発生を確認 |
| `ST-NFR-003-01` [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | 同一ローカルキャッシュ・同一パラメータ | 解析を2回実行し出力を比較する | `generated_at`等のタイムスタンプを除き全出力が同一 | CSV/GeoJSON/XLSXの差分比較（タイムスタンプ除外）で差分ゼロを確認 |
| `ST-NFR-003-02` [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | 複数保留組・通常候補・孤立登録を含む入力 | 列挙順を入れ替え突合・各出力を比較する | 組 ID・所属・並び・通常の仮コードが不変 | ログは組 ID 昇順・コード昇順、両 XLSX は通常/孤立の後に組 ID 順・内→外各コード順、GeoJSON は同じ組順と規定の組内順。既存行相対順維持。タイムスタンプを除き比較 |
| `ST-NFR-003-03` | [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | 同じバッチ入力、通常編集と複数の担当者指定削除 | 操作順・取消履歴・未選択の下書きだけを変え、最終選択と正規化後の根拠を揃えて両出力を比較 | 行数・行位置・内容・GeoJSON 分割と相対順が一致 | 既存の日時等の除外を適用して差分ゼロ。元のバッチファイル不変・下書き非出力も確認 |
| `ST-NFR-003-04` | [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | 同じバッチ・通常編集・手動削除判断・最終除外台帳 | 操作順と台帳配列順・解除後の下書きだけを変えて両申請出力と JSON を比較 | 申請行順・エビデンス全体・JSON 論理内容が一致 | records の種別/アクション/正式コードまたは座標順、既存日時等を除き差分ゼロ。新しい書出日時を追加しない |
| `ST-NFR-004-01` [NFR-004](20_SRS.md#nfr-004-アクセスマナー) | タイル取得実行 | HTTPリクエストヘッダーを確認する | User-Agent識別子が付与され、リクエスト間隔が守られる | リクエストログのUser-Agent・タイムスタンプ間隔を確認 |
| `ST-NFR-005-01` [NFR-005](20_SRS.md#nfr-005-処理時間目標) | 全175メッシュのタイル取得・解析 | 処理時間を計測する | タイル取得12時間以内・解析24時間以内が目安（ハードリミットではない） | 所要時間ログを確認（未達でも異常終了しないことも確認） |
| `ST-NFR-006-01` [NFR-006](20_SRS.md#nfr-006-可搬性環境) | [`01_environment.md`](01_environment.md)相当の環境 | 環境構築からパイプライン実行までを行う | 動作する | 完走を確認 |
| `ST-NFR-007-01` [NFR-007](20_SRS.md#nfr-007-ログ出力) | 各バッチ処理を実行 | ログ出力内容を確認する | 開始/終了時刻・処理対象・結果サマリ・エラー内容がログに含まれる | ログファイルの内容を照合 |
| `ST-NFR-008-01` [NFR-008](20_SRS.md#nfr-008-ui-レスポンス) | 作業用・公開用の各ビューアでボタンクリック・地図操作 | 応答時間を計測する | UI操作1秒以内、XLSXエクスポート5秒以内、ZIP10秒以内 | Claude in Chromeでの応答時間観測 |
| `ST-NFR-009-01` [NFR-009](20_SRS.md#nfr-009-観測可能性中間成果物の可視化) | 解析パイプライン実行中 | 各段階の中間GeoJSON（per-mesh・merged_peak.geojson）を地理院地図にドラッグ&ドロップする | ピーク・コル・ゾーンの対応関係が目視確認できる | Claude in Chromeによる目視確認 |
| `ST-NFR-010-01` [NFR-010](20_SRS.md#nfr-010-描画応答性連続操作の滑らかさ) | 最大1万件規模のマーカーを表示したビューア | パン・ズーム等の連続操作を行う | 60fps（フレーム≤16ms）を目標に滑らかに動作する | ブラウザ開発者ツールのパフォーマンス計測 |
| `ST-NFR-011-01` | [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性) | 対象版の README と参照先 | [手順文書チェックリスト](#381-手順文書チェックリスト) の全段階と入口からの到達性を確認 | 必要な手順・前提・完了/異常判定が揃う | チェックリスト全項目を満たし、未作成文書や非公開情報への依存がない |
| `ST-NFR-011-02` | [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性) | [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性) が定めるクリーン環境・全国入力を取得可能 | `ST-UR-008-01` と同じ実行で、入力準備から全成果物確認まで手順に従う | 文書外の補足なしで今回の全成果物を生成できる | [専用シナリオの合否・成果物確認表](#st-ur-008-01-環境構築成果物生成の一気通貫) を満たし、版・設定・ログ・警告等を記録 |
| `ST-NFR-011-03` | [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性) | 手順で準備した正常入力を持つ隔離作業領域 | 単独起動するフェーズの必須中間入力を退避して不足させ、停止を確認後、文書に従い前段から再生成して再実行 | 文書だけで原因と再実行位置を特定し、復元後に所定成果物を生成できる | 入力不足時の停止・是正後の出力を確認。不備ゲートを迂回せず、古い出力を成功の証拠にしない |
| `ST-NFR-011-04` | [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性) | 仕様上許容する警告/要確認で続行するデータ、および不備ゲートで停止するデータ | それぞれのログと出力を文書の完了条件で判断 | 続行時のドラフトと停止時の調査用出力を区別できる | 停止した実行を完遂と誤認せず、データ品質による正当な停止も未完了として原因を記録 |

`ST-NFR-011-02`・`ST-UR-008-01`・`ST-NFR-006-01` は実行を共有してよい。文書の存在確認だけで E2E 合格とはしない。
`ST-NFR-011-03`・`04` の具体的な入力は ST 実行手順で対象 FR の既定条件を再現する形に固定する。

#### 3.8.1 手順文書チェックリスト

`ST-NFR-011-01` は次の表を使い、[README](../README.md) から各手順へ到達できることと、各段階に操作・観測可能な完了条件があることを確認する。

| 段階 | 手順に必要な内容 |
|---|---|
| 環境構築 | 対象 OS・資源条件への参照、ブラウザの起動方法と通信条件（[SRS §6.2.5](20_SRS.md#625-作業用-html-ビューア)）、依存導入、対象版の取得、ビルド、設定と作業ディレクトリの準備。ネットワークが必要な段階と権限の前提 |
| 入力準備 | 標高タイル、SOTA リスト、行政区域データ、申請テンプレート等、各処理に必要な入力の取得元・配置・前処理・確認方法。必須と任意を区別する |
| バッチ実行 | タイル取得、行政区域前処理、解析・広域解析、統合、突合、作業用ビューア生成の順序、各段の前提・入力・出力・完了の確認方法 |
| ブラウザ操作 | 作業用ビューアの開き方、確認・編集、申請書 XLSX と申請エビデンス ZIP の保存、除外判断 JSON の保存・読込。ネットワーク条件は現行 URD/SRS に従う |
| 成果物確認 | 出力先と成果物の用途、内容を確認する方法、警告・要確認・申請保留と生成失敗の区別。生成物が申請ドラフトであることと公式提出が手動であること |
| 異常時の対処 | 失敗段階と原因をログ等から確認する方法、入力・設定の是正、再実行の開始段階、再利用できる入力/キャッシュと再生成が必要な出力の区別 |

#### 3.8.2 検出網羅性の基準セットと判定手順

`ST-NFR-001-02`〜`05` および全国/独立峰シナリオは本節を共通に用いる。
基準セット・期待メッシュ集合・各記録の値を試験前に揃え、未作成の場合は未実施と記録する。
本節の基準は試験用であり、製品の新たな入力や CSV 列を要求しない。

##### 全国処理範囲

[UR-001](10_URD.md#ur-001) の対象集合（採用時175メッシュ）について、期待集合と製品が使用するコード集合、
正常に処理を完了したコード集合と規則による省略集合（北方領土除外メッシュ）の和がそれぞれ一致すること。件数だけでは合格にしない。
期待集合は [第1次地域区画の出典](../ref/SOURCES.md#日本の国土にかかる第1次地域区画) と URD の対象範囲から、
省略集合は N03 行政区域データ（[ADR-SRS-053](decisions/ADR-SRS-053-northern-territories-tile-intersection-and-excluded-meshes.md) 参照）から、
製品の `params/` リストや当該実行ログを写さずに導き、出典版・除外根拠とともに事前固定する
（省略集合の導出に用いる N03 の版は、製品が使用した版と同一に固定する）。
[ADR-URD-009](decisions/ADR-URD-009-takeshima-exclusion.md) のメッシュ5531除外と、
[ADR-URD-005](decisions/ADR-URD-005-northern-territories-exclusion.md) のタイル単位除外・除外メッシュの省略は異なる粒度として適用し、
北方領土の除外を含有メッシュ全体の削除へ読み替えない。
①製品の対象リストと期待集合、②（正常完了集合 ∪ 省略集合）と期待集合を別々に照合し、重複・欠落を確認する。
正常完了集合と省略集合が重ならないこと（∩ = ∅）、製品が実行ログに残す省略集合が独立導出した省略集合と一致することも確認する。
各メッシュの完了ログと当該実行の per-mesh 成果物を対応付ける。省略集合に属するメッシュは per-mesh 成果物の代わりに省略ログで対応付ける。
必要な広域解析は [FR-023](20_SRS.md#fr-023-解析パイプライン制御) の条件で実施し、
実行済みの段階と既存仕様が認める省略の根拠を記録する。段階の欠落を通常の早期終了と混同しない。
過去の出力だけで完了したことにしない。入力タイルの欠如や異常終了（未整備による全欠如を含む）は別途記録し、対象メッシュを分母から消さない。
全国の最終 `merged_peak.csv` に北方領土6村ポリゴン内のピーク座標が0件であることを確認する。

##### 必須の記録

ST の基準セットには、実行前に次を確定して保存する。物理ファイル形式と照合ツールは後工程で決める。

| 記録 | 内容 |
|---|---|
| セット識別 | 版、作成/承認日、メンテナの承認、事前固定した内容のコミット ID または内容ハッシュ、対応 SRS 版、入力 DEM の取得情報と内容識別情報、期待メッシュ集合、省略集合とその導出に用いた N03 の版、設定（一次/最終フィルタ閾値を含む） |
| ピーク識別 | セット内で不変の ID、名称、地域、SOTA コードは存在する場合の参照情報。コードが異なるだけの同一地形は重複して数えない |
| 検出位置 | WGS84 の基準位置、出力ピーク座標の許容領域または許容座標集合、その根拠。同標高頂部や採用 DEM の解像度も考慮する |
| 期待する適格性 | ピークと Key コルの確認根拠、150m以上と判断した根拠、採用 DEM の種別/状態、対象区分、確認者 |
| 判定状態 | 必須、候補保留、対象外。保留/対象外には理由と根拠を記録する。必須セットとは分けて管理する |
| 履歴 | 追加・変更・対象外化の理由、対応した検出漏れ、再評価した入力版、承認記録 |

##### 正解を独立に確かめる

SOTA リスト、既存研究で扱った著名独立峰・陸地最高峰、未登録ピーク、既知の欠陥を候補源にする。
その候補が採用 DEM と仕様の閾値に適合することを、地形図・DEM の独立確認や検証対象とは独立した計算で裏付ける。
本製品のどの版・どの実行の出力も、候補位置の当たりを付ける用途にのみ使い、
適格性・許容位置・ピーク/Key コル・150m以上という正解の根拠にしない。
現行登録されているという理由だけで150m以上とみなすこともしない。
既存の親ピーク距離の代理調査は選定の手掛かりにとどめ、真の Key コルの位置・標高を確定する正解として流用しない。
陸地最高峰の手動リストは処理入力であり、そのリストに載っていること自体を適格性の独立証拠にしない。

DEM の誤差や欠損等で適格性が確定できない候補は保留とし、必須セットへ無理に入れない。
適格性を確認済みの峰に対する製品側の解析限界（広域解析の上限でもコルを解決できない等）は保留・対象外化の理由にしない。
ただし結果を見て必須から保留に移して同じ実行を合格にしない。必要区分の代替を含め、改版と再承認を要する。
実世界の地形と DEM 上の期待結果に差がある場合も、差と根拠を残す。誤差幅を一律に仮定して対象を除外しない。

##### 同一ピークの照合

各必須レコードの許容位置は、別の適格ピークを同じ山として拾わない大きさ・形に事前確定する。
一律の大きな距離半径や実行結果を見た領域拡張を使わず、座標の数値完全一致だけにも依存しない。
実際の許容値と単位・境界を含むかどうかは各レコードに必須とし、数値未確定のまま試験を始めない。

全必須レコードと最終出力行を一括照合し、各レコードに該当する行がちょうど1件で、
その行が他の必須レコードへ重複割当されない場合に一意とする。
0件は未検出、複数件や重複割当は曖昧とし、いずれも検出成功へ数えない。
曖昧な場合は基準の不備か出力の不備かを切り分けるが、解消前には合格を出さない。
近接した2峰、同標高頂部、メッシュ境界の例で誤照合・取り違えがないことも確認する。
基準外の余分な行を本再現率の分子に加えない。基準外だから偽陽性と断定することもしない。

##### 代表性と境界条件

実データの必須セットは以下の区分をすべて満たす。一つのレコードが複数区分を満たしてよい。
区分ごとの選定理由・件数・該当 ID を選定表に残す。件数は統計的な全国保証のサンプルサイズを意味しない。

| 区分 | 最低限の対象 |
|---|---|
| 地理的な広がり | 北海道・本州・四国・九州・それら以外の島嶼にそれぞれ1件以上 |
| 登録との独立性 | 現行 SOTA 登録のある適格ピークと、登録のない適格ピークをそれぞれ1件以上 |
| 解析窓と独立峰 | 通常解析で確定するものと、通常解析の窓外に Key コルがあり広域解析を必要とするものをそれぞれ1件以上 |
| 海面確定 | 自動で海面コルを確定する島の最高峰1件以上、および採用時の陸地最高峰リストの全対象 |
| DEM の違い | 基準ピーク画素または陸上 Key コル画素で DEM5 を採用する対象と、DEM10b フォールバックを採用する対象をそれぞれ1件以上。採用種別は下記のキャッシュ画素の証拠で確定する |
| 境界と近接 | タイル境界付近、1次メッシュ境界付近をそれぞれ1件以上。近接しているが別々に150m以上を満たす2峰を1組以上 |
| 既知の漏れ | これまでに確認して修正した検出漏れがあればすべて維持。事例がない場合だけ該当なしの根拠を残して空を許容し、空の必須区分による不合格の対象外とする。同一原因の代表化は根拠と承認を記録した改版でのみ行う |

DEM 区分の証拠は、固定した入力キャッシュの対象画素について、各候補種別のタイル ID・内容ハッシュ・
画素位置・存在/NODATA/有効値と、[FR-002](20_SRS.md#fr-002-dem-階層フォールバック) の優先順位に従った採用種別を記録する。
DEM5 と DEM10b の画素対応・解析グリッドも明示する。海面コルは実在画素として扱わず、ピーク画素を使う。
この区分は記録した画素での採用を意味し、山域全体が同じ DEM 種別であるとは主張しない。
製品の CSV に DEM 種別列の追加を要求せず、試験側で入力から確認する。
境界付近の定義（対象境界、距離/画素数・単位・境界の包含と根拠）は各レコードへ事前記録する。

候補例は [UR-007](10_URD.md#ur-007) の大山・東北の孤立峰や、
[ADR-SRS-019](decisions/ADR-SRS-019-land-summit-highest-peak-handling.md) の陸地最高峰である。
例示は期待値の確定を意味しない。SOTA コード・位置・コル・解析段階の期待値はセット作成時に一次データで確認する。
過去の研究資料の古い件数や代理距離から、特定の広域段階で必ず確定するとは決めない。

実データで正解を固定しにくい境界は、既知の地形を作った試験入力でも確認する。
150mちょうど、直下、直上、同標高頂部、近接2峰、境界を跨ぐピーク、窓外コルによる未確定保持から広域確定を対象とする。
地形の期待値は手計算等の独立した根拠を残し、UT/IT/ST の担当は [テスト方針書](02_test_policy.md) に従う。
この補助確認で全国実データの基準セットや処理範囲確認を置き換えない。150m直下の不採用は既存の閾値要件の検証であり、
本再現率の分母は150m以上の必須ピークに限る。

##### 基準の保守と再評価

初回セットは ST 実行前にメンテナが根拠・必須区分・照合条件を確認して版を確定する。
独立性は人数や役割でなく、正解の根拠が本製品のいずれの版の出力にも由来しないことを示す記録で担保する。
一人で作成・承認する場合も根拠資料と確認過程を残す。
基準レコード・期待メッシュ集合・選定表・根拠・許容位置を含む内容一式を、試験開始前にコミット ID または内容ハッシュで固定する。
開始記録にその識別子と入力版・コード版・設定を残し、終了時に同じ内容で照合したことを確認する。承認記録と開始記録も保存する。
改版時に識別子を使い回さない。単に実行後にハッシュを作っただけのものは事前確定の証拠にしない。
DEM/SOTA 参照版や解析規則・陸地最高峰リストの変更、新たな漏れの発見、照合の曖昧さを改版のトリガーとする。
SOTA リスト更新だけで地形上の必須ピークを消さず、地形・入力・仕様上の適格性を確認する。
不変の ID と旧版を保存し、削除や領域変更には独立した根拠とメンテナ承認を残す。

合格報告は入力版・基準版と事前固定識別子・対象コード版・設定・範囲照合結果・各基準の照合先・漏れ/曖昧/未確定の内訳を含める。
基準を変更した場合は旧版での結果も残し、新版で再実行/再照合した結果を別に示す。
再照合で保存済みの出力を使う場合も、新版の事前固定と開始記録を作り、元の解析の版・入力が新版と適合することを確認する。
その開始記録には照合対象の保存済み出力の内容ハッシュも残し、照合終了時に同じ内容であることを確認する。
解析のコード・入力・設定が変わる場合は解析を再実行する。再照合による合格を、解析前から新版を固定していた試験と混同しない。
基準に正当な訂正が必要でも、前の失敗をなかったことにしない。
既知の必須ピークの漏れは本基準の不合格であり、既知欠陥の登録だけで合格に読み替えない。
リリース可否の例外判断が別にある場合も、本基準の測定値・判定はそのまま報告する。
[テスト方針書 §5.3](02_test_policy.md#53-既知欠陥の許容基準) による既知欠陥の許容判断は別に記録し、本基準の漏れ数や不合格を合格へ書き換えない。

##### 合否判定の対照例

| 例 | 期待する判定 |
|---|---|
| 175件の完了ログだが1コード重複・別の1コード欠落 | 全国処理範囲が不一致で不合格 |
| 北方領土除外メッシュを正常完了集合に含めた（除外メッシュが解析中心として実行された） | 意図的除外と正常完了の混同で不合格 |
| 本土を含むメッシュ（例: 知床・羅臼・根室を含むメッシュ）を省略集合に含めた | 誤省略で不合格。北方領土除外は「含有メッシュ全体の削除」ではない |
| 必須100件中99件を検出、1件だけ消失 | 再現率99%、漏れ1件で不合格 |
| 必須全件の位置があるが、1件のコルが未確定 | そのレコードは成功に数えず不合格 |
| 必須レコードの許容位置に出力行が2件、または1件だが最終閾値未満 | 前者は曖昧、後者は適格性不一致で、いずれも成功に数えず不合格 |
| 近接する2基準ピークに同じ出力行を割当 | 重複割当で不合格。別の2峰を検出したことにしない |
| 必須全件が一意に確定、広域代表の stability が `-` | stability だけを理由に不合格にしない。範囲と他条件も成立すれば合格 |
| 再現率100%だが島嶼区分に必須レコードがない | 基準セットの必須区分不足で合格にしない |
| 必須レコード0件、または許容位置の数値が未確定 | 基準妥当性を満たさず合格にしない。0/0 を100%としない |
| 出力を見て許容半径を広げる、未検出を対象外へ移す | 同じ基準での合格にしない。独立根拠・改版・再承認・再評価が必要 |
| 必須全件検出だが、新しい基準外の漏れが確認された | 未知の漏れゼロを主張せず、欠陥として処置し回帰基準へ追加 |

## 4. 第2部: UR単位の妥当性確認シナリオ

FRに一意に分解できるURは、対応する第1部テストケースへの参照で足りるため専用シナリオを設けない（詳細は[URカバレッジ表](#5-urカバレッジ表)参照）。以下はFR横断・分解不能なURの専用E2Eシナリオ。

<a id="st-ur-001-01-全国176メッシュ実行"></a>

### ST-UR-001-01: 全国処理範囲と基準ピークの検出

| 項目 | 内容 |
|---|---|
| 対象UR | [UR-001](10_URD.md#ur-001) |
| 対応要件 | [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定)。測定経路は [FR-023](20_SRS.md#fr-023-解析パイプライン制御)・[FR-022](20_SRS.md#fr-022-コル充足判定)・[FR-014](20_SRS.md#fr-014-広域結合解析オーケストレーション)・[FR-008](20_SRS.md#fr-008-per-mesh-csv-統合) |
| 前提条件 | [基準セットと判定手順](#382-検出網羅性の基準セットと判定手順)に従い独立導出した期待集合（採用時175メッシュ）と基準セットを事前固定。対象タイルを準備し、コード版・入力版・設定・基準識別子を開始記録へ残す |
| 手順 | 全対象の通常解析、必要な広域解析、範囲指定なしの全国最終統合まで実行。`ST-NFR-001-02`〜`04` を適用し、（正常完了集合 ∪ 省略集合）と期待集合の一致と最終 `merged_peak.csv` の基準全件を検査 |
| 期待結果 | 製品リスト/（正常完了集合 ∪ 省略集合）が期待集合と一致し（正常完了集合と省略集合は重ならない）、全必須ピークが一意にコル確定・最終閾値以上で検出される。最終 `merged_peak.csv` に北方領土6村ポリゴン内のピークが0件である |
| 合否判定基準 | 全国範囲・基準妥当性・漏れ0件/再現率100%の全条件。件数だけ、著名峰だけのサンプル、未確定候補の存在では合格にしない。既存の出力全数の精度や偽陽性等の判定は省略しない |
| 記録 | 本節の共通手順に従い、当該実行のログ/成果物・実施/省略段階と根拠・各ピークの対応行・必須区分ごとの件数/漏れ数・未確定/曖昧を保存 |

### ST-UR-007-01: 独立峰の広域解析から最終検出まで

| 項目 | 内容 |
|---|---|
| 対象UR | [UR-007](10_URD.md#ur-007) |
| 対応要件 | [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定)。測定経路は [FR-023](20_SRS.md#fr-023-解析パイプライン制御)・[FR-022](20_SRS.md#fr-022-コル充足判定)・[FR-014](20_SRS.md#fr-014-広域結合解析オーケストレーション)・[FR-008](20_SRS.md#fr-008-per-mesh-csv-統合) |
| 前提条件 | 全国シナリオと同じ事前固定基準に、通常窓外に陸上 Key コルがあり広域解析を必要とする適格ピークが1件以上。海面確定区分も全件含める |
| 手順 | `ST-UR-001-01` と実行を共有し、通常解析での未確定保持、必要な広域段階と統合後の対応行を追跡。海面確定区分は既存規則による確定経路を確認 |
| 期待結果 | 広域を必要とする必須峰と海面確定区分の全必須峰が、全国最終出力へ一意にコル確定・最終閾値以上で残る |
| 合否判定基準 | 対象区分の漏れ0件・再現率100%。per-mesh だけの検出、上限到達後もコル未確定の行は成功にしない。製品の上限を理由に必須ピークを保留へ移さない |
| 記録 | 全国シナリオの記録に加え、対象 ID ごとの通常/広域段階と最終行への対応、海面確定の根拠。通常/広域の期待経路は製品出力から正解を逆算せず事前確定する |

### ST-UR-008-01: 環境構築〜成果物生成の一気通貫

| 項目 | 内容 |
|---|---|
| 対象UR | [UR-008](10_URD.md#ur-008) |
| 対応要件 | [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性)。動作環境は [NFR-006](20_SRS.md#nfr-006-可搬性環境)、ログは [NFR-007](20_SRS.md#nfr-007-ログ出力) |
| 前提条件 | [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性) に定めるクリーン環境（ツール専用依存・設定・参照データ・キャッシュ・中間/最終出力・ブラウザ状態が事前にない）。対象版のソース/文書から開始し、必要な権限と通信を使用できる。実データの削除ではなく別作業領域等で隔離する |
| 手順 | README とリンク先の手順だけで [チェックリスト](#381-手順文書チェックリスト) の環境構築・入力準備・バッチ実行・ブラウザ操作・成果物確認を実行。[UR-001](10_URD.md#ur-001) の解析対象175メッシュを解析し（北方領土除外メッシュは省略される）全国一括で突合。各段の中間成果物を確認し、編集・両申請出力・除外判断保存/読込まで実施する |
| 期待結果 | 口頭補足・非公開設定・ソース読解などの暗黙知なしに、バッチとブラウザの必要成果物を今回生成・内容確認できる |
| 合否判定基準 | 下表の全項目を満たし、文書外補足ゼロ。文書化された利用者入力は逸脱に数えない。停止条件が未解消・必要成果物が欠落なら合格にしない。小規模実行や既存の統合出力の持込はクリーン E2E の代替にしない |
| 記録 | 入力データの版・取得日・識別情報、設定値、文書の版、実行環境、ログ、警告と成果物の確認結果。具体的な入力版・コマンド・確認対象サミット・期待値は実行前に固定する |

| 成果物 | 本 E2E での最低限の内容確認 |
|---|---|
| 突合済み GeoJSON | FeatureCollection として読め、metadata の入力版・ソフトウェア版・生成日時が今回の記録と一致する。ST 実行手順で事前指定した確認対象のサミットを含む |
| 両サミット一覧 XLSX | ワークブックを開け、規定の列見出しがある。事前指定したサミットを突合済み GeoJSON/ビューアの状態と照合する |
| 作業用ビューア | データファイルとともに開け、事前指定したサミットが地図・ポップアップに表示される。記載された編集とエクスポート操作を実行できる |
| 申請書 XLSX | ワークブックを開け、規定の申請シート・列見出しがある。申請行数がビューアの今回の対象件数と一致する。対象がある場合は事前指定した候補のアクション・編集内容を照合する |
| ZIP | 展開でき、SRS の全構成物が揃う。各 GeoJSON は FeatureCollection として読め、metadata が今回の突合済み GeoJSON から複製されている |
| PNG | 当該解析の出力先に自動生成され、画像として開け、対象範囲の地形が表示される |
| 除外判断 JSON | JSON として読め、明示的な読込・確認操作で保存時と同じ台帳内容を復元できる |

ZIP の構成は [SRS §6.2.8](20_SRS.md#628-申請エビデンス-zipパッケージング仕様) を正とし、除外判断 JSON は同梱しない。
申請行・各分類フィーチャ・除外判断 records は仕様どおり0件でもよく、非空の除外候補を要求しない。
各 I/F の全項目/全行の正しさ・数値精度・検出網羅性は第1部の対応ケースと対応 UR のシナリオへ委ねる。
公開用ビューアの配信は [FR-020](20_SRS.md#fr-020-公開用ビューア配信) の検証で扱い、公式提出とともに本シナリオの終点に含めない。

文書にない補足が必要なら文書不備とする。外部サービス停止や入力品質による正当な不備ゲート発動は未完了と原因を記録し、文書/機能不備と区別する。
仕様上許容する警告・要確認だけで生成失敗とはしない。根拠付き設定見直しは [SRS §2.2.1](20_SRS.md#221-設定可能項目) に従い、是正後の設定と再実行結果を残す。
文書修正・入力是正後は本確認の記載手順で取得したキャッシュ等を再利用できる。取得履歴と使用段階を残し、未変更の準備段階の証跡と再実行を合わせて判定する。
記載手順では作れないキャッシュの持込や過去の出力の流用を、今回の成果物生成の成功として扱わない。

### ST-UR-015-01: 除外判断の端末移行と再利用

| 項目 | 内容 |
|---|---|
| 対象UR | [UR-015](10_URD.md#ur-015) |
| 前提条件 | 通常の追加・削除・バンド変更候補、従属削除、前回だけ存在する対象を含むデータと2つのブラウザ環境 |
| 手順 | 一方で理由付き除外と JSON 保存、保存領域を消去して復元。別 PC へ移して読込後に申請書/ZIP を出力。条件を変えた次回入力でも継承する |
| 期待結果 | 同対象・同条件の除外を再現し、不在・条件変化は記録を保持して未適用通知。申請行は除外され、理由と候補はエビデンスに残る |
| 合否判定基準 | ファイル往復の対象/条件/理由、除外後の申請行、全件エビデンスを照合。公開用は理由を閲覧できるが判断操作なし。詳細は第1部の関連ケース |

## 5. URカバレッジ表

[`20_SRS.md` §12 要求追跡マトリクス](20_SRS.md#12-要求追跡マトリクス)と同一形式。UR-009は削除済みのため列から除外。「テストケース」列は代表参照ID（詳細は各URが✅を持つFR/NFRの第1部テストケースを参照）。

| UR | 内容 | 対応FR/NFR（✅） | テストケース |
|---|---|---|---|
| [UR-001](10_URD.md#ur-001) | 全国ピーク網羅検出 | [FR-001](20_SRS.md#fr-001-標高タイル事前取得), [FR-002](20_SRS.md#fr-002-dem-階層フォールバック), [FR-003](20_SRS.md#fr-003-標高デコードnodata-処理), [FR-004](20_SRS.md#fr-004-33メッシュ結合解析オーケストレーション), [FR-005](20_SRS.md#fr-005-ピーク候補検出), [FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用), [FR-017](20_SRS.md#fr-017-n03-行政区域前処理データ準備), [NFR-002](20_SRS.md#nfr-002-メモリ使用量), [NFR-005](20_SRS.md#nfr-005-処理時間目標), [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | `ST-NFR-001-02`〜`05` ＋ `ST-UR-001-01`（専用シナリオ）＋各FR/NFRテストケース |
| [UR-002](10_URD.md#ur-002) | プロミネンス≥150m抽出 | [FR-006](20_SRS.md#fr-006-コル検出プロミネンス計算), [FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用), [FR-008](20_SRS.md#fr-008-per-mesh-csv-統合), [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定), [NFR-002](20_SRS.md#nfr-002-メモリ使用量) | `ST-FR-006-*`, `ST-FR-007-*`, `ST-FR-008-08`, `ST-FR-008-09`, `ST-NFR-001-01` |
| [UR-003](10_URD.md#ur-003) | 新規・削除・変更識別 | [FR-008](20_SRS.md#fr-008-per-mesh-csv-統合), [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定), [FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成), [FR-017](20_SRS.md#fr-017-n03-行政区域前処理データ準備), [FR-018](20_SRS.md#fr-018-per-mesh-ピーク候補-geojson-統合), [NFR-002](20_SRS.md#nfr-002-メモリ使用量) | `ST-FR-009-*`（主）、`ST-FR-008-*`, `ST-FR-016-*`, `ST-FR-018-*` |
| [UR-004](10_URD.md#ur-004) | 申請書XLSX生成 | [FR-011](20_SRS.md#fr-011-申請書-xlsx-生成), [FR-017](20_SRS.md#fr-017-n03-行政区域前処理データ準備), [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | `ST-FR-011-*`, `ST-NFR-003-*` |
| [UR-005](10_URD.md#ur-005) | エビデンスXLSX出力 | [FR-006](20_SRS.md#fr-006-コル検出プロミネンス計算), [FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用), [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定), [FR-012](20_SRS.md#fr-012-サミット一覧申請内容反映版生成), [FR-021](20_SRS.md#fr-021-申請エビデンス-zip-生成), [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | `ST-FR-012-*`, `ST-FR-021-*`, `ST-NFR-003-*` |
| [UR-006](10_URD.md#ur-006) | GeoJSON・HTMLビューア生成 | [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定), [FR-013](20_SRS.md#fr-013-html-ビューア生成), [FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成), [FR-018](20_SRS.md#fr-018-per-mesh-ピーク候補-geojson-統合), [FR-019](20_SRS.md#fr-019-html-ビューア機能仕様), [FR-020](20_SRS.md#fr-020-公開用ビューア配信), [FR-021](20_SRS.md#fr-021-申請エビデンス-zip-生成)（公開用データの供給）, [NFR-008](20_SRS.md#nfr-008-ui-レスポンス) | `ST-FR-013-*`, `ST-FR-019-*`, `ST-FR-020-*`, `ST-FR-021-*`, `ST-NFR-008-01` |
| [UR-007](10_URD.md#ur-007) | 独立峰漏れなく検出 | [FR-014](20_SRS.md#fr-014-広域結合解析オーケストレーション), [FR-022](20_SRS.md#fr-022-コル充足判定), [FR-023](20_SRS.md#fr-023-解析パイプライン制御), [NFR-002](20_SRS.md#nfr-002-メモリ使用量), [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | `ST-UR-007-01` ＋ `ST-NFR-001-02`〜`05`、`ST-FR-014-*`, `ST-FR-022-*` |
| [UR-008](10_URD.md#ur-008) | 環境構築〜成果物生成 | [NFR-006](20_SRS.md#nfr-006-可搬性環境), [NFR-007](20_SRS.md#nfr-007-ログ出力), [NFR-011](20_SRS.md#nfr-011-手順文書による環境構築成果物生成の完遂性) | `ST-UR-008-01`（専用シナリオ）＋`ST-NFR-006-01`, `ST-NFR-007-01`, `ST-NFR-011-01`〜`04` |
| [UR-010](10_URD.md#ur-010) | 利用規約遵守取得 | [FR-001](20_SRS.md#fr-001-標高タイル事前取得), [NFR-004](20_SRS.md#nfr-004-アクセスマナー) | `ST-FR-001-02`, `ST-NFR-004-01` |
| [UR-011](10_URD.md#ur-011) | 成果物に出典明示 | [FR-012](20_SRS.md#fr-012-サミット一覧申請内容反映版生成), [FR-019](20_SRS.md#fr-019-html-ビューア機能仕様), [FR-020](20_SRS.md#fr-020-公開用ビューア配信), [FR-021](20_SRS.md#fr-021-申請エビデンス-zip-生成) | `ST-FR-012-01`, `ST-FR-019-01`, `ST-FR-020-01`, `ST-FR-021-01` |
| [UR-012](10_URD.md#ur-012) | 地形目視確認画像 | [FR-015](20_SRS.md#fr-015-標高地形図出力) | `ST-FR-015-*` |
| [UR-013](10_URD.md#ur-013) | 中間成果物地図可視化 | [FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成), [FR-018](20_SRS.md#fr-018-per-mesh-ピーク候補-geojson-統合), [NFR-009](20_SRS.md#nfr-009-観測可能性中間成果物の可視化) | `ST-FR-016-*`, `ST-FR-018-*`, `ST-NFR-009-01` |
| [UR-014](10_URD.md#ur-014) | ビューア対話応答性 | [FR-019](20_SRS.md#fr-019-html-ビューア機能仕様), [NFR-010](20_SRS.md#nfr-010-描画応答性連続操作の滑らかさ) | `ST-FR-019-01`, `ST-NFR-010-01` |
| [UR-015](10_URD.md#ur-015) | 申請除外・判断の持ち運び | [FR-019](20_SRS.md#fr-019-html-ビューア機能仕様), [FR-011](20_SRS.md#fr-011-申請書-xlsx-生成), [FR-012](20_SRS.md#fr-012-サミット一覧申請内容反映版生成), [FR-021](20_SRS.md#fr-021-申請エビデンス-zip-生成), [FR-020](20_SRS.md#fr-020-公開用ビューア配信), [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | `ST-UR-015-01` ＋ `ST-FR-019-14`〜`34`、`ST-FR-011-06`、`ST-FR-012-04`、`ST-FR-021-05`、`ST-FR-020-07`、`ST-NFR-003-04` |
