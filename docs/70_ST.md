# システムテスト仕様書（ST）

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-07-12 |
| 最終更新日 | 2026-09-19 |
| ステータス | ドラフト |
| 参照 SRS | [`20_SRS.md`](20_SRS.md) |
| 参照 URD | [`10_URD.md`](10_URD.md) |

---

## 目次

1. [目的・スコープ](#1-目的スコープ)
2. [テストケース記述規約](#2-テストケース記述規約)
3. [第1部: FR/NFR単位のシステムテストケース](#3-第1部-frnfr単位のシステムテストケース)
4. [第2部: UR単位の妥当性確認シナリオ](#4-第2部-ur単位の妥当性確認シナリオ)
5. [URカバレッジ表](#5-urカバレッジ表)

---

## 1. 目的・スコープ

本書はシステムテスト（ST）レベルのテストケースを定める。[ADR-ST-001](decisions/ADR-ST-001-two-part-st-no-separate-at-family.md) の方針に従い、以下の2部構成とする。

- **第1部**: [`20_SRS.md`](20_SRS.md) のFR/NFR単位のシステムテストケース（SRS検証）
- **第2部**: [`10_URD.md`](10_URD.md) のUR単位の妥当性確認シナリオ（URD妥当性確認）。FRに一意に分解できるURは第1部のテストケースへの参照とし、FR横断・分解不能なURのみ専用シナリオを記述する

テストレベル定義・自動/手動の分担・テスト環境・合否判定基準・カバレッジ粒度の方針は [`02_test_policy.md`](02_test_policy.md) を正本とし、本書では重複記述しない。

## 2. テストケース記述規約

- **テストケースID命名規則**: [`02_test_policy.md` §6](02_test_policy.md#6-テストケースid命名の大枠) の共通規則に従う。第1部は `ST-FR-<番号>-<連番>` / `ST-NFR-<番号>-<連番>`、第2部は `ST-UR-<番号>-<連番>`
- **記述テンプレート**: 各テストケースは以下を記述する。
  - **前提条件**: テスト実行前に満たすべき状態（入力データ・環境等）
  - **手順**: 実行する操作・コマンド
  - **期待結果**: 正しい場合に得られるべき出力・状態
  - **合否判定基準**: 期待結果との照合方法
- **カバレッジ粒度**: [`02_test_policy.md` §5.2](02_test_policy.md#52-カバレッジ粒度の正本) の既定方針に従い、リスクの高いFR/NFRは複数ケース、その他は代表ケース1件とする

## 3. 第1部: FR/NFR単位のシステムテストケース

### 3.1 行政区域前処理コンポーネント

#### FR-017: N03 行政区域前処理（データ準備）

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-017-01` [7.2.3](20_SRS.md#723-n03-行政区域-zip) 準拠のN03行政区域ZIPが配置済み、出力ファイル群未生成 | 行政区域前処理を実行する | N03前処理済み地域GeoJSON（60地域）・市区町村GeoJSON（約2,000件）・北方領土除外タイルリストが生成される | 出力3種のファイル存在・フィーチャ数（60地域）を確認 |
| `ST-FR-017-02`（異常系） N03行政区域ZIPが1件も配置されていない | 行政区域前処理を実行する | エラー終了する（[20_SRS.md §4.1 異常系](20_SRS.md#41-行政区域前処理コンポーネント)参照） | 異常終了・エラーメッセージの出力を確認 |
| `ST-FR-017-03`（境界値） 複数バージョンのN03 ZIP（`N03-YYYYMMDD_GML.zip`）が配置されている | 行政区域前処理を実行する | YYYYMMDDが最大のZIPが採用される | ログに記録された採用ZIP名のYYYYMMDDが最大値と一致することを確認 |

### 3.2 タイル取得コンポーネント

#### FR-001: 標高タイル事前取得

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-001-01` 1次メッシュコードリスト（1件以上）を指定、北方領土除外タイルリスト生成済み | タイル事前取得を実行する | 対象メッシュをカバーするDEM5a/5b/5c/10bタイルがローカルキャッシュに保存される | キャッシュディレクトリのファイル存在・タイル座標範囲を確認 |
| `ST-FR-001-02`（境界値） 北方領土（択捉・国後・色丹）を含むbounding box範囲でタイル取得を実行 | タイル事前取得を実行する | 北方領土除外タイルリストに含まれるタイル（DEM5a/5b/5cはZ15完全一致、DEM10bはZ14変換後）へのHTTPリクエストが発行されない | リクエストログに除外対象タイルが含まれないことを確認 |
| `ST-FR-001-03`（異常系） 北方領土除外タイルリストが未生成 | タイル事前取得を実行する | エラー終了する | 異常終了を確認 |
| `ST-FR-001-04`（異常系） 地理院タイルサーバーがHTTP 429/503を返す状況を再現し、リトライ上限を超過させる | タイル事前取得を実行する | エラーを出力して処理を中断する | 異常終了・エラーメッセージを確認 |

### 3.3 パイプライン制御コンポーネント

#### FR-023: 解析パイプライン制御

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-023-01` フェーズ2実行後、[FR-022](20_SRS.md#fr-022-コル充足判定)が未確定ピークゼロを返す状態 | パイプライン制御を実行する | フェーズ4へ移行する（フェーズ3をスキップ） | フェーズ4（[FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定)）が実行されたことを確認 |
| `ST-FR-023-02`（境界値） フェーズ2実行後、未確定ピークが残りN=6（上限到達）まで解消しない状態 | パイプライン制御を実行する | N=6完了後、未確定ピーク残存のままフェーズ4へ移行する | N=7へのエスカレーションが発生しないこと・フェーズ4移行を確認 |
| `ST-FR-023-03`（異常系） フェーズ2内の呼び出しFR（例: [FR-004](20_SRS.md#fr-004-33メッシュ結合解析オーケストレーション)）がエラー終了する状況を再現 | パイプライン制御を実行する | フェイルファースト方針によりFR-023も即時終了する | 異常終了・後続フェーズが実行されないことを確認 |

### 3.4 地形解析エンジン

#### FR-004: 3×3メッシュ結合解析オーケストレーション

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-004-01` 対象メッシュの3×3範囲のローカルキャッシュタイルが存在 | 3×3結合解析を実行する | 解析範囲標高グリッド生成→[FR-015](20_SRS.md#fr-015-標高地形図出力)→[FR-005](20_SRS.md#fr-005-ピーク候補検出)/[FR-006](20_SRS.md#fr-006-コル検出プロミネンス計算)→[FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用)→[FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成)→meshset書き出しの順に処理される | 各段階の出力ファイル（標高地形図PNG・per-mesh CSV・per-mesh GeoJSON・meshset）の生成順序をログで確認 |
| `ST-FR-004-02`（異常系） 対象メッシュのローカルキャッシュタイルが1枚も存在しない | 3×3結合解析を実行する | エラーを出力して処理を中止する | 異常終了・エラーメッセージを確認 |

#### FR-014: 広域結合解析オーケストレーション

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-014-01` [FR-022](20_SRS.md#fr-022-コル充足判定)がkey_col_resolved=falseピークを検出、N=4で呼び出し | 広域結合解析を実行する | N×N（N=4）メッシュの4コーナーパターン（NW/NE/SW/SE）で解析が実行される | 4コーナー分の広域per-mesh CSV・meshsetが生成されることを確認 |
| `ST-FR-014-02`（異常系） ウィンドウ範囲の存在メッシュにローカルタイルが1枚も存在しない | 広域結合解析を実行する | エラーを出力して処理を中止する | 異常終了・該当メッシュコードを含むエラーメッセージを確認 |
| `ST-FR-014-03`（境界値） 4コーナー全パターンを試行してもkey_col_resolvedが確定しないピーク | 広域結合解析を実行する | 当該ピークはkey_col_resolved=falseのまま各コーナーパターンの広域per-mesh CSVに出力される | 出力CSVでkey_col_resolved=falseを確認 |

#### FR-002: DEM 階層フォールバック

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-002-01` DEM5aが有効値のピクセル | DEMフォールバック処理を実行する | DEM5aが採用される | 採用DEM階層を確認 |
| `ST-FR-002-02`（境界値） DEM5a/5b/5cが全てNODATA、DEM10bのみ有効値のピクセル | DEMフォールバック処理を実行する | DEM10bへフォールバックする | 採用DEM階層を確認 |
| `ST-FR-002-03`（境界値） 全DEM階層がNODATAのピクセル | DEMフォールバック処理を実行する | RGB(128,0,0)を返す（正常分岐） | 出力RGB値を確認 |

#### FR-003: 標高デコード・NODATA 処理

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-003-01` 通常の正標高RGB値（例: 標高500m相当） | 標高デコードを実行する | `標高 = (R×65536+G×256+B)/100.0` の計算式通りにデコードされる | デコード結果を数値照合 |
| `ST-FR-003-02`（境界値） NODATAピクセル（R=128, G=0, B=0） | 標高デコードを実行する | -9999mとしてデコードされる | デコード結果が-9999であることを確認 |
| `ST-FR-003-03`（境界値） 海面下ピクセル（中間値x = 0x800001以上、符号変換対象） | 標高デコードを実行する | `x - 2^24` を適用した負の標高値としてデコードされ、後段のタイル処理で海面（0m）に統一される | デコード直後の負値・後段統一後の0mを両方確認 |
| `ST-FR-003-04`（境界値） NODATA判定の境界値（x = 0x800000ちょうど） | 標高デコードを実行する | NODATA（-9999m）として判定される（符号変換より先にNODATA判定が適用される） | デコード結果が-9999であることを確認 |

#### FR-015: 標高地形図出力

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-015-01` 解析範囲標高グリッドに陸地画素が存在 | 標高地形図出力を実行する | gist_earth+陰影起伏方式（[ADR-SRS-047](decisions/ADR-SRS-047-terrain-color-scheme-gist-earth-hillshade.md)）でPNGが生成される | 出力PNGの存在・長辺6000px縮小を確認 |
| `ST-FR-015-02`（境界値） 標高0m以下（海面・NODATA統一後）の画素 | 標高地形図出力を実行する | 白（`#FFFFFF`）でマスクされる | 該当画素の色を確認 |
| `ST-FR-015-03`（異常系） 解析範囲標高グリッドに陸地画素（標高>0m）が1つも存在しない（`vmax<=0`） | 標高地形図出力を実行する | 異常終了する（2026-07-12追加。[FR-015異常系](20_SRS.md#fr-015-標高地形図出力)参照） | 異常終了を確認 |

#### FR-005: ピーク候補検出

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-005-01` 解析範囲標高グリッド（既知のピーク地形を含む） | ピーク候補検出を実行する | 局所的な地表面最高点がピーク候補として検出される | 検出されたピーク候補座標を既知の地形と照合 |
| `ST-FR-005-02`（境界値） 同標高の隣接ピクセルが複数存在する状況 | ピーク候補検出を実行する | タイブレーク規則（ピクセルインデックス昇順）で1つが候補となる | 決定論的に同一結果が得られることを再実行で確認（[NFR-003](20_SRS.md#nfr-003-再現性決定論的出力)） |

#### FR-006: コル検出・プロミネンス計算

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-006-01` 2つの連結成分が接触するピクセルを含む解析範囲 | コル検出を実行する | 接触ピクセルがコルとして検出され、プロミネンス=ピーク標高-コル標高が算出される | コル座標・プロミネンス値を照合 |
| `ST-FR-006-02`（境界値） 陸地連結成分が外周1px海面ボーダーに接触する状況 | コル検出を実行する | `key_col_resolved=false`、`col_elev`/`prominence`は空欄、`col_lat`/`col_lon`=0.0 | 出力sentinel値の組み合わせを確認 |
| `ST-FR-006-03`（境界値） 走査ループ完了後、外周1px海面ボーダーへの接触フラグが立っていない連結成分（島の最高峰） | コル検出を実行する | 海面確定規則によりKeyコル=海面(0m)・`key_col_resolved=true`、`col_lat`/`col_lon`=0.0 | 出力sentinel値の組み合わせ（`key_col_resolved=true` + 座標0.0）を確認 |

#### FR-007: per-mesh CSV 出力（プロミネンス閾値適用）

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-007-01` `key_col_resolved=true`、プロミネンス一次フィルタ閾値以上のピーク | per-mesh CSV出力を実行する | CSVに出力される | 出力CSVにレコード存在を確認 |
| `ST-FR-007-02`（境界値） `key_col_resolved=true`、プロミネンスが一次フィルタ閾値ちょうど | per-mesh CSV出力を実行する | 「以上」条件のため出力される | 出力CSVにレコード存在を確認 |
| `ST-FR-007-03`（境界値） `key_col_resolved=false`（プロミネンス未確定）のピーク | per-mesh CSV出力を実行する | フィルタ適用されず無条件に出力される | 出力CSVにレコード存在を確認 |
| `ST-FR-007-04`（境界値） 解析対象メッシュ範囲外（フリンジ部分）で検出されたピーク | per-mesh CSV出力を実行する | 地理的範囲フィルタにより出力対象外となる | 出力CSVに該当レコードが存在しないことを確認 |

#### FR-016: ピーク域ポリゴン生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-016-01` フィルタ後ピーク候補・コル情報リスト（解析範囲内で完結） | ピーク域ポリゴン生成を実行する | `area_complete=true` のアクティベーションゾーン・delete判定ゾーンポリゴンが生成される | 出力GeoJSONのポリゴン・`area_complete`値を確認 |
| `ST-FR-016-02`（境界値） ポリゴンが解析範囲外で途切れる状況 | ピーク域ポリゴン生成を実行する | `area_complete=false` として出力される（正常な状態フラグ） | 出力GeoJSONの`area_complete=false`を確認 |

### 3.5 統合・突合コンポーネント

#### FR-008: per-mesh CSV 統合

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-008-01` 複数のper-mesh CSVに同一ピーク座標が重複出現 | per-mesh CSV統合を実行する | 代表採用ロジック（key_col_resolved=true優先→col_elev最高優先→通常モード優先→analysis_id昇順）で1件に統合される | 統合結果のレコードが代表採用ロジック通りであることを確認 |
| `ST-FR-008-02`（境界値） 通常モードで`key_col_resolved=false`、広域モードで`key_col_resolved=true`の同一ピーク | per-mesh CSV統合を実行する | 広域モード結果が代表として採用される | 統合結果の`key_col_resolved`・`analysis_id`を確認 |
| `ST-FR-008-03`（境界値、issue #16関連） 通常モード行、`analysis_count ≠ expected_count`の状況 | per-mesh CSV統合を実行する | `stability=unstable`と判定される | 出力CSVの`stability`列を確認 |
| `ST-FR-008-04`（境界値） 陸地最高峰リスト対象ピーク（富士山等）で自動走査は`key_col_resolved=false` | per-mesh CSV統合を実行する | `key_col_resolved=true`（Keyコル=海面0m）に更新されるが`stability`は`unstable`のまま維持される | 出力CSVの`key_col_resolved`と`stability`の組み合わせを確認 |
| `ST-FR-008-05`（異常系） per-mesh ピーク候補CSVが1件も存在しない | per-mesh CSV統合を実行する | エラー終了する | 異常終了を確認 |

#### FR-018: per-mesh ピーク候補 GeoJSON 統合

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-018-01` 同一ピーク座標で`area_complete=true`のポリゴンが存在 | GeoJSON統合を実行する | `area_complete=true`のポリゴンが採用される | 出力GeoJSONを確認 |
| `ST-FR-018-02`（境界値） `area_complete=true`のポリゴンが見つからないピーク | GeoJSON統合を実行する | `area_complete=false`のポリゴンがフォールバックとして採用される | 出力GeoJSONを確認 |

#### FR-022: コル充足判定

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-022-01` `merged_peak.csv`に未確定ピーク（`key_col_resolved=false`）が存在 | コル充足判定を実行する | 未確定有無「残あり」を返し、`key_col_unresolved_peaks-<N>.csv`が出力される | 戻り値・出力ファイルを確認 |
| `ST-FR-022-02` `merged_peak.csv`の全ピークが`key_col_resolved=true` | コル充足判定を実行する | 未確定有無「ゼロ」を返す | 戻り値を確認 |
| `ST-FR-022-03`（境界値） `merged_peak.csv`がピーク件数ゼロ（空CSV） | コル充足判定を実行する | 未確定有無「ゼロ」を返して正常終了する | 戻り値を確認（エラーにならないこと） |
| `ST-FR-022-04`（異常系） `merged_peak.csv`が存在しない | コル充足判定を実行する | エラー終了する | 異常終了を確認 |

#### FR-009: SOTAリスト突合・match_status 判定

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-009-01`（境界値） ピークのアクティベーションゾーン内に既存SOTAサミット座標が存在 | SOTAリスト突合を実行する | `peak.match_status="matched"` | 出力GeoJSONのmatch_statusを確認 |
| `ST-FR-009-02`（境界値） ピークのdelete判定ゾーン内かつアクティベーションゾーン外にサミット座標が存在 | SOTAリスト突合を実行する | `peak.match_status="dominant"`、当該サミットが削除候補 | 出力GeoJSONのmatch_status・delete候補を確認 |
| `ST-FR-009-03`（境界値） delete判定ゾーン内にも既存サミットが存在しないピーク | SOTAリスト突合を実行する | `peak.match_status="new"` | 出力GeoJSONのmatch_statusを確認 |
| `ST-FR-009-04`（境界値） 既存サミット座標がいずれのAZ・delete判定ゾーンにも含まれない | SOTAリスト突合を実行する | `summit.match_status="unmatched"`（要確認）、停止せず続行 | 出力GeoJSONのmatch_statusを確認、処理継続を確認 |
| `ST-FR-009-05`（異常系・不備ゲート1） `unmatched`件数が要確認サミット件数しきい値を超える | SOTAリスト突合を実行する | 不備ゲートとして異常終了する（GeoJSON/XLSXは出力してから停止） | 異常終了・出力ファイル存在の両方を確認 |
| `ST-FR-009-06`（異常系・不備ゲート2） AZまたはdelete判定ゾーンポリゴンの`area_complete=false`が1件以上 | SOTAリスト突合を実行する | 不備ゲートとして異常終了する | 異常終了・出力ファイル存在を確認 |
| `ST-FR-009-07`（異常系・不備ゲート3） `key_col_resolved=false`のピークが1件以上（N=6エスカレーション後も未解消） | SOTAリスト突合を実行する | 不備ゲートとして異常終了する | 異常終了・出力ファイル存在を確認 |
| `ST-FR-009-08`（境界値） 仮サミットコード採番: 同一エリアで標高・プロミネンス・座標が異なる複数ピーク | SOTAリスト突合を実行する | 標高降順→プロミネンス降順→peak_lat降順→peak_lon昇順の順序で`A00`から採番される | 採番結果の順序を照合 |
| `ST-FR-009-09`（境界値） 同一エリア内で仮サミットコード連番上限（prefix `99`）を超過 | SOTAリスト突合を実行する | エラーメッセージ（地域コード・超過件数を含む）を出力して異常終了する | 異常終了・エラーメッセージ内容を確認 |

### 3.6 可視化生成コンポーネント

#### FR-013: HTML ビューア生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-013-01` `merged_summit.geojson`が正常生成済み | HTMLビューア生成を実行する | `merged_viewer.html`と`merged_viewer_data.js`が生成され、データファイルにGeoJSONの内容が格納される | 2ファイルの存在とデータファイルの内容を確認 |
| `ST-FR-013-02`（異常系） [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定)が不備ゲートとして異常終了した状態 | HTMLビューア生成を実行する | 実行をスキップしHTML・データファイルとも生成しない | HTML・データファイル未生成を確認 |

### 3.7 申請書生成 UI

#### FR-019: HTML ビューア機能仕様

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-019-01` `merged_viewer.html`をブラウザで開く | 地図表示・マーカー・背景タイル切替を操作する | Leaflet地図が表示され、feature_typeに応じた形状（summit=●/peak=▲/key_col=▼）でマーカーが描画され、`© 国土地理院`（リンク付き）と「加工して作成」の旨が常時表示される | Claude in Chromeによる目視確認 |
| `ST-FR-019-02` `category=add`かつ`match_status=new`のピークを選択 | 山岳名JP/EN入力フィールドに入力する | 入力内容がlocalStorageに保存される | 再読み込み後も入力内容が保持されていることを確認 |
| `ST-FR-019-03` 検索ボックスにサミットコード・山岳名の一部を入力 | サジェスト候補から選択する | 該当サミットへ地図ズーム移動しポップアップが開く | Claude in Chromeによる目視確認 |
| `ST-FR-019-04`（境界値） 新しいパイプライン実行で`generated_at`が変わった状態でビューアを開く | 前回入力内容の引き継ぎ確認ダイアログを操作する | 「引き継ぐ」選択時は前回入力が新しい`generated_at`キーに引き継がれる | localStorageの内容を確認 |

#### FR-011: 申請書 XLSX 生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-011-01` `category=add`のピーク（山岳名JP入力済み） | 「申請書」ボタンをクリックする | アクション列=追加、B〜J列がテンプレート通りにマッピングされたXLSXがダウンロードされる | ダウンロードXLSXの列内容を照合 |
| `ST-FR-011-02`（境界値） `category=delete`・`category=band_change`のフィーチャが混在 | 「申請書」ボタンをクリックする | 各アクションに応じた行が出力され、`no_change`・`review`は出力されない | 出力XLSXの行数・アクション種別を確認 |

#### FR-012: サミット一覧（申請内容反映版）生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-012-01` 山岳名編集済みのビューア状態 | 「申請エビデンス」ボタンをクリックする（FR-012はインライン実行） | `merged_summit_revised.xlsx`が編集後の山岳名を反映して生成され、最後に「出典」シート（地理院タイルを加工して作成した旨と出典リンク）を持つ | ZIP内XLSXの内容と「出典」シートを確認 |

#### FR-021: 申請エビデンス ZIP 生成

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-021-01` `merged_summit.geojson`が5カテゴリ全てのフィーチャを含む | 「申請エビデンス」ボタンをクリックする | ZIPに`merged_summit_revised.xlsx`と5つのカテゴリ別GeoJSON（add/delete/band_change/no_change/review）が同梱され、各GeoJSONの`metadata`に`attribution`・`source_url`・`license_url`が入っている | ダウンロードZIPの内容物と各GeoJSONの`metadata`を確認 |
| `ST-FR-021-02`（境界値） あるカテゴリのフィーチャが0件 | 「申請エビデンス」ボタンをクリックする | 該当カテゴリのGeoJSONも空FeatureCollectionとしてZIPに同梱される | ZIP内の該当ファイルが空FeatureCollectionであることを確認 |

#### FR-020: 公開用ビューア配信

| ID | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|
| `ST-FR-020-01` 公開用データ置き場に申請エビデンスGeoJSON5本を配置し、静的ホスティングが配信済み | 公開用ビューアのURLを開く | 編集UI・エクスポート・localStorageを除く閲覧機能で5本の内容が表示され、`© 国土地理院`（リンク付き）と「加工して作成」の旨が表示され、配信されるGeoJSONが配置に用いたZIPの同梱物と同一 | 配信ファイルと配置に用いたZIP同梱物の差分比較（`metadata.attribution`を含む） + Claude in Chromeによる目視確認 |
| `ST-FR-020-02` リリース版と開発版の両方を配信済み | 両方のURLを開く | 両方が同時に閲覧でき、画面上でどちらかを判別できる | Claude in Chromeによる目視確認 |
| `ST-FR-020-03`（異常系） 公開用データ5本のうち1本が取得できない状態で配信 | 公開用ビューアのURLを開く | 残り4本の内容が表示され、欠落した1本を利用者が判別できる | Claude in Chromeによる目視確認 |
| `ST-FR-020-04`（境界値） `metadata.generated_at`が異なるZIPに由来する5本を混在配置して配信 | 公開用ビューアのURLを開く | 異なる解析結果の混在として利用者に提示される | Claude in Chromeによる目視確認 |

### 3.8 非機能要件

| ID | 対象NFR | 前提条件 | 手順 | 期待結果 | 合否判定基準 |
|---|---|---|---|---|---|
| `ST-NFR-001-01` [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定) | 統合後のピークデータ | 全出力ピークのプロミネンスを検査する | 全てプロミネンス最終フィルタ閾値以上 | `merged_peak.csv`を走査し閾値未満のレコードが無いことを確認 |
| `ST-NFR-002-01` [NFR-002](20_SRS.md#nfr-002-メモリ使用量) | 9メッシュ最大解析を実行 | メモリ使用量を監視しながら解析を実行する | 動作前提マシン（62.72GiB+スワップ48GiB）で完走する | 解析完了・OOM未発生を確認 |
| `ST-NFR-003-01` [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | 同一ローカルキャッシュ・同一パラメータ | 解析を2回実行し出力を比較する | `generated_at`等のタイムスタンプを除き全出力が同一 | CSV/GeoJSON/XLSXの差分比較（タイムスタンプ除外）で差分ゼロを確認 |
| `ST-NFR-004-01` [NFR-004](20_SRS.md#nfr-004-アクセスマナー) | タイル取得実行 | HTTPリクエストヘッダーを確認する | User-Agent識別子が付与され、リクエスト間隔が守られる | リクエストログのUser-Agent・タイムスタンプ間隔を確認 |
| `ST-NFR-005-01` [NFR-005](20_SRS.md#nfr-005-処理時間目標) | 全175メッシュのタイル取得・解析 | 処理時間を計測する | タイル取得12時間以内・解析24時間以内が目安（ハードリミットではない） | 所要時間ログを確認（未達でも異常終了しないことも確認） |
| `ST-NFR-006-01` [NFR-006](20_SRS.md#nfr-006-可搬性環境) | [`01_environment.md`](01_environment.md)相当の環境 | 環境構築からパイプライン実行までを行う | 動作する | 完走を確認 |
| `ST-NFR-007-01` [NFR-007](20_SRS.md#nfr-007-ログ出力) | 各バッチ処理を実行 | ログ出力内容を確認する | 開始/終了時刻・処理対象・結果サマリ・エラー内容がログに含まれる | ログファイルの内容を照合 |
| `ST-NFR-008-01` [NFR-008](20_SRS.md#nfr-008-ui-レスポンス) | 作業用・公開用の各ビューアでボタンクリック・地図操作 | 応答時間を計測する | UI操作1秒以内、XLSXエクスポート5秒以内、ZIP10秒以内 | Claude in Chromeでの応答時間観測 |
| `ST-NFR-009-01` [NFR-009](20_SRS.md#nfr-009-観測可能性中間成果物の可視化) | 解析パイプライン実行中 | 各段階の中間GeoJSON（per-mesh・merged_peak.geojson）を地理院地図にドラッグ&ドロップする | ピーク・コル・ゾーンの対応関係が目視確認できる | Claude in Chromeによる目視確認 |
| `ST-NFR-010-01` [NFR-010](20_SRS.md#nfr-010-描画応答性連続操作の滑らかさ) | 最大1万件規模のマーカーを表示したビューア | パン・ズーム等の連続操作を行う | 60fps（フレーム≤16ms）を目標に滑らかに動作する | ブラウザ開発者ツールのパフォーマンス計測 |

## 4. 第2部: UR単位の妥当性確認シナリオ

FRに一意に分解できるURは、対応する第1部テストケースへの参照で足りるため専用シナリオを設けない（詳細は[URカバレッジ表](#5-urカバレッジ表)参照）。以下はFR横断・分解不能なURの専用E2Eシナリオ。

### ST-UR-001-01: 全国176メッシュ実行

| 項目 | 内容 |
|---|---|
| 対象UR | [UR-001](10_URD.md#ur-001) |
| 前提条件 | 日本全国1次メッシュコードリスト（land総数176・うち解析対象175、北方領土・竹島除く）のタイルが事前取得済み |
| 手順 | `./build/findsummits params/mesh_list_japan.txt` で全メッシュ解析を実行する |
| 期待結果 | 175メッシュ全てが解析完了し、ピーク候補が網羅的に検出される（北方領土・竹島所在のサミット候補は除外） |
| 合否判定基準 | 解析完了ログの対象メッシュ数=175を確認。既知の主要山岳（富士山等）がピーク候補に含まれることをサンプル照合 |

### ST-UR-008-01: 環境構築〜成果物生成の一気通貫

| 項目 | 内容 |
|---|---|
| 対象UR | [UR-008](10_URD.md#ur-008) |
| 前提条件 | クリーンな環境（[`01_environment.md`](01_environment.md)相当） |
| 手順 | [README](../README.md) のセットアップ・使い方の手順に従い、ビルド→タイル取得→解析→統合→成果物生成までを実行する |
| 期待結果 | ドキュメント記載の手順のみで、追加の暗黙知なしに全成果物（申請書XLSX・エビデンスZIP・HTMLビューア等）が生成できる |
| 合否判定基準 | 手順の逸脱・エラーなく完走し、全成果物ファイルが生成されることを確認 |

## 5. URカバレッジ表

[`20_SRS.md` §12 要求追跡マトリクス](20_SRS.md#12-要求追跡マトリクス)と同一形式。UR-009は削除済みのため列から除外。「テストケース」列は代表参照ID（詳細は各URが✅を持つFR/NFRの第1部テストケースを参照）。

| UR | 内容 | 対応FR/NFR（✅） | テストケース |
|---|---|---|---|
| [UR-001](10_URD.md#ur-001) | 全国ピーク網羅検出 | [FR-001](20_SRS.md#fr-001-標高タイル事前取得), [FR-002](20_SRS.md#fr-002-dem-階層フォールバック), [FR-003](20_SRS.md#fr-003-標高デコードnodata-処理), [FR-004](20_SRS.md#fr-004-33メッシュ結合解析オーケストレーション), [FR-005](20_SRS.md#fr-005-ピーク候補検出), [FR-017](20_SRS.md#fr-017-n03-行政区域前処理データ準備), [NFR-002](20_SRS.md#nfr-002-メモリ使用量), [NFR-005](20_SRS.md#nfr-005-処理時間目標) | `ST-UR-001-01`（専用シナリオ）＋各FR/NFRテストケース |
| [UR-002](10_URD.md#ur-002) | プロミネンス≥150m抽出 | [FR-006](20_SRS.md#fr-006-コル検出プロミネンス計算), [FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用), [NFR-001](20_SRS.md#nfr-001-精度プロミネンス判定), [NFR-002](20_SRS.md#nfr-002-メモリ使用量) | `ST-FR-006-*`, `ST-FR-007-*`, `ST-NFR-001-01` |
| [UR-003](10_URD.md#ur-003) | 新規・削除・変更識別 | [FR-008](20_SRS.md#fr-008-per-mesh-csv-統合), [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定), [FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成), [FR-017](20_SRS.md#fr-017-n03-行政区域前処理データ準備), [FR-018](20_SRS.md#fr-018-per-mesh-ピーク候補-geojson-統合), [NFR-002](20_SRS.md#nfr-002-メモリ使用量) | `ST-FR-009-*`（主）、`ST-FR-008-*`, `ST-FR-016-*`, `ST-FR-018-*` |
| [UR-004](10_URD.md#ur-004) | 申請書XLSX生成 | [FR-011](20_SRS.md#fr-011-申請書-xlsx-生成), [FR-017](20_SRS.md#fr-017-n03-行政区域前処理データ準備), [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | `ST-FR-011-*`, `ST-NFR-003-01` |
| [UR-005](10_URD.md#ur-005) | エビデンスXLSX出力 | [FR-006](20_SRS.md#fr-006-コル検出プロミネンス計算), [FR-007](20_SRS.md#fr-007-per-mesh-csv-出力プロミネンス閾値適用), [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定), [FR-012](20_SRS.md#fr-012-サミット一覧申請内容反映版生成), [FR-021](20_SRS.md#fr-021-申請エビデンス-zip-生成), [NFR-003](20_SRS.md#nfr-003-再現性決定論的出力) | `ST-FR-012-01`, `ST-FR-021-*`, `ST-NFR-003-01` |
| [UR-006](10_URD.md#ur-006) | GeoJSON・HTMLビューア生成 | [FR-009](20_SRS.md#fr-009-sotaリスト突合match_status-判定), [FR-013](20_SRS.md#fr-013-html-ビューア生成), [FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成), [FR-018](20_SRS.md#fr-018-per-mesh-ピーク候補-geojson-統合), [FR-019](20_SRS.md#fr-019-html-ビューア機能仕様), [FR-020](20_SRS.md#fr-020-公開用ビューア配信), [FR-021](20_SRS.md#fr-021-申請エビデンス-zip-生成)（公開用データの供給）, [NFR-008](20_SRS.md#nfr-008-ui-レスポンス) | `ST-FR-013-*`, `ST-FR-019-*`, `ST-FR-020-*`, `ST-FR-021-*`, `ST-NFR-008-01` |
| [UR-007](10_URD.md#ur-007) | 独立峰漏れなく検出 | [FR-014](20_SRS.md#fr-014-広域結合解析オーケストレーション), [FR-022](20_SRS.md#fr-022-コル充足判定), [FR-023](20_SRS.md#fr-023-解析パイプライン制御), [NFR-002](20_SRS.md#nfr-002-メモリ使用量) | `ST-FR-014-*`, `ST-FR-022-*` |
| [UR-008](10_URD.md#ur-008) | 環境構築〜成果物生成 | [NFR-006](20_SRS.md#nfr-006-可搬性環境), [NFR-007](20_SRS.md#nfr-007-ログ出力) | `ST-UR-008-01`（専用シナリオ）＋`ST-NFR-006-01`, `ST-NFR-007-01` |
| [UR-010](10_URD.md#ur-010) | 利用規約遵守取得 | [FR-001](20_SRS.md#fr-001-標高タイル事前取得), [NFR-004](20_SRS.md#nfr-004-アクセスマナー) | `ST-FR-001-02`, `ST-NFR-004-01` |
| [UR-011](10_URD.md#ur-011) | 成果物に出典明示 | [FR-012](20_SRS.md#fr-012-サミット一覧申請内容反映版生成), [FR-019](20_SRS.md#fr-019-html-ビューア機能仕様), [FR-020](20_SRS.md#fr-020-公開用ビューア配信), [FR-021](20_SRS.md#fr-021-申請エビデンス-zip-生成) | `ST-FR-012-01`, `ST-FR-019-01`, `ST-FR-020-01`, `ST-FR-021-01` |
| [UR-012](10_URD.md#ur-012) | 地形目視確認画像 | [FR-015](20_SRS.md#fr-015-標高地形図出力) | `ST-FR-015-*` |
| [UR-013](10_URD.md#ur-013) | 中間成果物地図可視化 | [FR-016](20_SRS.md#fr-016-ピーク域ポリゴン生成), [FR-018](20_SRS.md#fr-018-per-mesh-ピーク候補-geojson-統合), [NFR-009](20_SRS.md#nfr-009-観測可能性中間成果物の可視化) | `ST-FR-016-*`, `ST-FR-018-*`, `ST-NFR-009-01` |
| [UR-014](10_URD.md#ur-014) | ビューア対話応答性 | [FR-019](20_SRS.md#fr-019-html-ビューア機能仕様), [NFR-010](20_SRS.md#nfr-010-描画応答性連続操作の滑らかさ) | `ST-FR-019-01`, `ST-NFR-010-01` |
